## 🔍 QA Review Round 2 — PR #313

**Data:** 2026-03-19
**Reviewer:** QA Agent
**Escopo:** Re-análise após correção dos 12 findings do Round 1

---

### Verificação dos Fixes

| # | Finding | Severidade | Corrigido? | Introduziu bugs? | Notas |
|---|---------|-----------|-----------|------------------|-------|
| 1 | paidAmount sempre multiplica por 100 | 🟡 WARN | ✅ | Não | Heurística frágil removida. Agora usa `Math.round(numValue * 100)` sempre, tanto em `parseWebhookEvent` quanto em `fetchSettlementData`. Consistente. |
| 2 | nsuCode removido como fallback de bankNumber | 🟡 WARN | ✅ | Não | Chain agora é `payload.bankNumber ?? payload.nossoNumero ?? ""`. Correto — nsuCode não é bankNumber. |
| 3 | retry 1x no 401 do authenticatedFetch | 🟡 WARN | ✅ | Não | Invalida cache, obtém token novo, retry 1x. Se retry também dá 401, lança erro. Também trata 403 no retry. Sem risco de loop infinito. |
| 4 | JSDoc no _webhookSecret | 🔵 INFO | ✅ | Não | `/** Unused. Santander validates webhooks by unique URL per provider, not HMAC. */` — claro e conciso. |
| 5 | validateWebhook verifica covenantCode | 🟡 WARN | ✅ | Não | Verifica `covenantCode` do payload contra o do provider. Defensivo: só rejeita se payload tem covenant E não bate. Se payload não traz covenant, passa (correto para payloads parciais). |
| 6 | retry P2034 no getNextBankNumber | 🔵 INFO | ✅ | Não | Loop com MAX_RETRIES=3, catch P2034 com `continue`, throw em outros erros. `throw` final para satisfazer TypeScript. Implementação correta. |
| 7 | crypto.randomUUID para nsuCode | 🔵 INFO | ✅ | Não | `crypto.randomUUID().replace(/-/g, "").slice(0, 20)` — 20 chars hex, satisfaz pattern `^[A-Za-z0-9]{1,20}$`. Unicidade garantida pelo UUID v4. |
| 8 | provider.sandbox no webhook route | 🔵 INFO | ✅ | Não | Agora usa `{ sandbox: provider.sandbox, companyId: provider.companyId }`. Fix de 1 linha, correto. |
| 9 | payerDocumentNumber como string (não parseInt) | 🟡 WARN | ✅ | Não | `getBankSlipPdf` agora passa `payerDocumentNumber` como string direto no JSON body. Sem parseInt. Seguro para CPFs e CNPJs. |
| 10 | limite de tamanho no upload PEM | 🔵 INFO | ✅ | Não | Check `file.size > 50_000` antes de ler, com toast de erro e reset do input. 50KB é generoso para PEM (~2-4KB típico). |
| 11 | (verificação do pattern factory) | 🔵 INFO | N/A | N/A | Não requeria correção no Round 1. |
| 12 | console.warn em placeholders de endereço | 🟡 WARN | ✅ | Não | `console.warn` quando `!input.customer.address?.zipCode`. Mensagem clara sobre risco em produção. |

---

### Novos Findings

**Nenhum finding novo identificado.**

Todos os fixes foram implementados de forma limpa, sem introduzir side effects, regressões ou novos code smells. O código está consistente com os patterns existentes.

---

### Veredicto

## ✅ APPROVED

Todos os 11 findings acionáveis (1-10, 12) foram corrigidos corretamente. Nenhuma correção introduziu bugs ou regressões. PR pronta para merge.
