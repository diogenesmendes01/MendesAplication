# High-Level Goal: Weather App

Develop a simple web application that displays the current weather for a user-specified city. The application should allow the user to input a city name and then display the temperature, a weather description (e.g., "clear sky", "partly cloudy"), and an icon representing the weather condition. Use a public weather API for data retrieval.
