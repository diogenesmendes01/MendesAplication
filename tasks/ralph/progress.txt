# Ralph Progress — MendesERP (sac-omnichannel)

## Codebase Patterns
- Project lives in `/workspaces/app/erp/` — all npm/npx commands must run from there
- Next.js 14.2.35 with App Router, TypeScript strict mode
- TailwindCSS 3.x with Shadcn/UI (New York style, default config)
- Prisma 6.x with `prisma-client-js` generator (classic, not new `prisma-client`)
- Prisma singleton in `src/lib/prisma.ts` — import from `@/lib/prisma`
- Shadcn/UI components in `src/components/ui/` — use Sonner for toasts (not the old Toaster)
- Quality checks: `npx tsc --noEmit` for typecheck, `npm run lint` for ESLint
- PostgreSQL needs `sudo service postgresql start` before db operations in devcontainer
- Prisma compound unique with nullable fields: use `findFirst` + `create` instead of `upsert`
- `.env` is gitignored; `.env.example` has the template
- Working directory for git commands: `/workspaces/app/` (root repo)
- Prisma seed config goes in `package.json` under `"prisma": { "seed": "npx tsx prisma/seed.ts" }`
- Use `upsert` in seed scripts for idempotent re-runs
- Shared enums (Status, UserRole) are defined at schema level and reused across models
- Auth utilities in `src/lib/auth.ts` — import `hashPassword`, `comparePassword`, `generateAccessToken`, `generateRefreshToken`, `verifyAccessToken`, `getCurrentUser`
- Session helpers in `src/lib/session.ts`: `getSession()`, `requireSession()`, `requireAdmin()`
- RBAC helpers in `src/lib/rbac.ts`: `canAccessCompany()`, `requireCompanyAccess()`
- Server actions use `"use server"` directive and live alongside their pages (e.g. `sac/tickets/actions.ts`)
- Company context: `useCompany()` hook from `@/contexts/company-context` gives `selectedCompanyId`, `selectedCompany`, `companies`
- Company-scoped server actions accept `companyId` param and validate access via `requireCompanyAccess(companyId)`
- Navigation items defined in `navItems` array in sidebar.tsx — add new nav links there
- Listing pages: client component with useCallback for loadData, useEffect to trigger, search resets to page 1
- Form dialogs: reuse same Dialog for create/edit, distinguish via `editingId` state
- Audit logging on all mutations via AuditLog model
- Abstract interfaces for email provider in `src/lib/email.ts`
- Design doc: `docs/plans/2026-03-02-sac-omnichannel-design.md`
- PRD: `tasks/prd-sac-omnichannel.md`
- Redis devcontainer feature: `ghcr.io/itsmechlark/features/redis-server:1` — after devcontainer rebuild, redis-server is auto-started
- REDIS_URL env var: `redis://localhost:6379` — used by BullMQ and ioredis

## 2026-03-02 - US-048
- Installed Redis via devcontainer feature (`ghcr.io/itsmechlark/features/redis-server:1`)
- Added REDIS_URL=redis://localhost:6379 to .env.example
- Verified redis-cli ping returns PONG
- Typecheck passes
- Files changed: `.devcontainer/devcontainer.json`, `erp/.env.example`
- **Learnings for future iterations:**
  - The devcontainer feature `ghcr.io/itsmechlark/features/redis-server:1` installs and auto-starts Redis on port 6379
  - In a running container, the feature won't take effect until rebuild — install redis-server manually with apt for immediate testing
  - `.env` is gitignored; always update `.env.example` for template and `.env` locally for runtime
---

## 2026-03-02 - US-049
- Configured BullMQ infrastructure with 5 named queues: email-inbound, email-outbound, whatsapp-inbound, whatsapp-outbound, sla-check
- Created `erp/src/lib/queue.ts` with Redis connection config (parses REDIS_URL env var) and queue exports
- Created `erp/src/lib/workers/base.ts` with `createWorker()` factory function that logs job processing
- Created `erp/src/lib/workers/index.ts` with worker instances for all 5 queues and graceful shutdown
- Added `"workers"` npm script: `npx tsx src/lib/workers/index.ts`
- Added bullmq@5.70.1 dependency (ioredis@5.9.3 comes as transitive dep)
- Files changed: `erp/package.json`, `erp/package-lock.json`, `erp/src/lib/queue.ts`, `erp/src/lib/workers/base.ts`, `erp/src/lib/workers/index.ts`
- **Learnings for future iterations:**
  - BullMQ connection config uses plain object `{ host, port, password, maxRetriesPerRequest: null }` — maxRetriesPerRequest must be null for workers
  - Workers run via `npx tsx` from erp/ directory — the `@/` path alias works because tsx respects tsconfig paths
  - `createWorker()` factory in base.ts handles logging, error handling, and events — use it for all new workers
  - Queue names are constants in `QUEUE_NAMES` object — import from `@/lib/queue`
  - Graceful shutdown in index.ts listens for SIGINT/SIGTERM and closes all workers
---

## 2026-03-02 - US-050
- Created `docker-compose.yml` at project root with evolution-api service (atydoc/evolution-api:latest) on port 8080
- Added EVOLUTION_API_URL and EVOLUTION_API_KEY to `erp/.env.example` with explanatory comments
- Docker compose uses named volumes for persistence (evolution_instances, evolution_store)
- Files changed: `docker-compose.yml`, `erp/.env.example`
- **Learnings for future iterations:**
  - Evolution API uses `AUTHENTICATION_API_KEY` env var internally — map from our `EVOLUTION_API_KEY`
  - Start Evolution API with `docker compose up -d evolution-api` from project root
  - Evolution API default port is 8080
---

## 2026-03-02 - US-051
- Added 10 new enums to schema.prisma: ChannelType, MessageDirection, MessageOrigin, SlaType, RefundStatus, RefundPaymentMethod, RefundAttachmentType, RefundInvoiceAction, PayableOrigin, InvoiceType
- Added CANCELLED to ProposalStatus enum
- InvoiceStatus already had CANCELLED
- Fixed VALID_STATUS_TRANSITIONS in propostas/actions.ts to include CANCELLED state
- Applied schema changes via `npx prisma db push`
- Files changed: `erp/prisma/schema.prisma`, `erp/src/app/(app)/comercial/propostas/actions.ts`
- **Learnings for future iterations:**
  - Adding values to enums used in Record<EnumType, ...> maps will cause TS errors — update all exhaustive maps
  - ProposalStatus transitions map is in `comercial/propostas/actions.ts` line ~91
  - PostgreSQL needs to be started with `sudo service postgresql start` before db push
---

## 2026-03-02 - US-052
- Created AdditionalContact model: id (cuid), clientId, name, role?, email?, whatsapp?, createdAt
- Added relation Client.additionalContacts (1:N, onDelete Cascade)
- Added sample clients (ABC Tecnologia, XYZ Serviços) and 3 additional contacts to seed
- Used createMany for bulk contact creation with idempotent check
- Files changed: `erp/prisma/schema.prisma`, `erp/prisma/seed.ts`
- **Learnings for future iterations:**
  - Seed used `findMany` + check length for idempotent createMany (since createMany doesn't support upsert)
  - Client unique constraint is `cpfCnpj_companyId` — use this for upsert in seeds
---

## 2026-03-02 - US-053
- Created Channel model with all fields: id, companyId, type (ChannelType), name, config (Json), isActive, lastSyncUid, lastSyncUidSent, lastSyncAt, createdAt, updatedAt
- Added Company.channels relation (1:N, onDelete Cascade)
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-054
- Created SlaConfig model with unique constraint [companyId, type, priority, stage]
- Added Company.slaConfigs relation (1:N, onDelete Cascade)
- Seeded 9 SLA configs per company: 6 ticket SLAs (3 priorities x 2 stages) + 3 refund SLAs
- Files changed: `erp/prisma/schema.prisma`, `erp/prisma/seed.ts`
- **Learnings for future iterations:**
  - Prisma compound unique with nullable fields doesn't work well with `upsert` — use `findFirst` + `create` pattern instead
  - SLA deadlines in minutes: Alta first_reply=30, resolution=240; Media 120/1440; Baixa 480/2880
  - Refund SLAs use priority=null since they don't vary by priority
---

## 2026-03-02 - US-055
- Created Attachment model with ticketMessageId/ticketId (both optional), fileName, fileSize, mimeType, storagePath
- Added named relations: TicketMessage.attachments ("TicketMessageAttachments"), Ticket.attachments ("TicketAttachments")
- Created `erp/uploads/` directory with `.gitkeep`; added gitignore rules for uploads content
- Files changed: `erp/prisma/schema.prisma`, `.gitignore`, `erp/uploads/.gitkeep`
---

## 2026-03-02 - US-056
- Created Refund model with all fields: id, ticketId, companyId, requestedById, approvedById, executedById, amount, paymentMethod, bank details, pixKey, status, rejectionReason, timestamps, SLA fields, invoice action
- Created RefundAttachment model with type (RefundAttachmentType), file info, uploadedById
- Added named relations on User: refundsRequested/Approved/Executed
- Added refunds relation to Ticket and Company
- Note: accountPayable and creditNote back-relations to be added in US-059
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-057
- Added channelId, contactId, slaFirstReply, slaResolution, slaBreached, tags fields to Ticket
- Added channel (Channel), contact (AdditionalContact) relations to Ticket
- Added tickets[] back-relations to Channel and AdditionalContact
- Seed continues working without errors
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-058
- Added channel (ChannelType?), direction (MessageDirection), origin (MessageOrigin), externalId, contactId, isInternal fields to TicketMessage
- Added contact relation (AdditionalContact) with named relation "TicketMessageContact"
- Added @@unique([externalId, channel]) for deduplication
- Used `--accept-data-loss` flag for db push when adding unique constraint on existing table
- Files changed: `erp/prisma/schema.prisma`
- **Learnings for future iterations:**
  - Adding unique constraints to existing tables may require `--accept-data-loss` flag with `prisma db push`
  - Named relations needed when model has multiple references to same target (e.g., AdditionalContact referenced by both Ticket and TicketMessage)
---

## 2026-03-02 - US-059
- Added origin (PayableOrigin), refundId (unique) to AccountPayable with Refund relation
- Added type (InvoiceType), cancelledAt, cancellationReason, refundId (unique), originalInvoiceId to Invoice
- Added self-relation "CreditNote" on Invoice for credit notes
- Added accountPayable and creditNote back-relations to Refund (completing US-056 deferred work)
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-060
- Created contacts-actions.ts with 4 server actions: listAdditionalContacts, createAdditionalContact, updateAdditionalContact, deleteAdditionalContact
- All actions validate company access via requireCompanyAccess
- Create/update validate at least email or whatsapp is provided
- All mutations include audit logging via logAuditEvent
- Access validation checks contact's client.companyId matches requested companyId
- Files changed: `erp/src/app/(app)/comercial/clientes/[id]/contacts-actions.ts`
---

## 2026-03-02 - US-061
- Created AdditionalContacts component with list, create/edit dialog, and delete confirmation
- Shows name, role, email, whatsapp for each contact with edit/delete buttons
- Integrated into client detail page between profile card and timeline
- Toast notifications for success/error on all operations
- Browser verification not available (no dev-browser MCP tool)
- Files changed: `erp/src/app/(app)/comercial/clientes/[id]/additional-contacts.tsx`, `.../[id]/page.tsx`
---
