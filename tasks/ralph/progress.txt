# Ralph Progress — MendesERP (erp-multi-empresa)

## Codebase Patterns
- Project lives in `/workspaces/app/erp/` — all npm/npx commands must run from there
- Next.js 14.2.35 with App Router, TypeScript strict mode
- TailwindCSS 3.x with Shadcn/UI (New York style, default config)
- Prisma 6.x with `prisma-client-js` generator (classic, not new `prisma-client`)
- Prisma singleton in `src/lib/prisma.ts` — import from `@/lib/prisma`
- Shadcn/UI components in `src/components/ui/` — use Sonner for toasts (not the old Toaster)
- Quality checks: `npx tsc --noEmit` for typecheck, `npm run lint` for ESLint
- `.env` is gitignored; `.env.example` has the template
- Working directory for git commands: `/workspaces/app/` (root repo)
- Prisma seed config goes in `package.json` under `"prisma": { "seed": "npx tsx prisma/seed.ts" }` (prisma.config.ts `seed` not supported in Prisma 6.19)
- PostgreSQL needs manual setup: `sudo apt-get install postgresql`, `sudo su -c "..." postgres` to configure
- Use `upsert` in seed scripts for idempotent re-runs
- Shared enums (Status, UserRole) are defined at schema level and reused across models
- Auth utilities in `src/lib/auth.ts` — import `hashPassword`, `comparePassword`, `generateAccessToken`, `generateRefreshToken`, `verifyAccessToken`, `getCurrentUser`
- Auth middleware `withAuth` in `src/lib/auth-middleware.ts` — wraps API handlers with JWT validation
- Refresh token stored in httpOnly cookie "refreshToken", refresh endpoint at POST `/api/auth/refresh`
- Access token stored in non-httpOnly cookie "accessToken" so middleware can check auth state
- Next.js middleware at `src/middleware.ts` handles auth redirects: unauthenticated → /login
- Login API at POST `/api/auth/login` — validates credentials, sets both cookies
- Label component in `src/components/ui/label.tsx` for form labels
- Authenticated pages use `(app)` route group — layout at `src/app/(app)/layout.tsx`
- Sidebar component at `src/components/sidebar.tsx`, Header at `src/components/header.tsx`
- Navigation items defined in `navItems` array in sidebar.tsx — add new nav links there
- Root page `/` redirects to `/dashboard`
- Server actions use `"use server"` directive and live alongside their pages (e.g. `configuracoes/empresas/actions.ts`)
- Session helpers in `src/lib/session.ts`: `getSession()`, `requireSession()`, `requireAdmin()` — read accessToken cookie for auth in server actions
- CNPJ validation in `src/lib/cnpj.ts`: `isValidCnpj()`, `stripCnpj()`, `formatCnpj()` — validates check digits
- Server actions use `PaginatedResult<T>` type for paginated listings (page, pageSize, total, totalPages, data)
- CNPJs are stored formatted (XX.XXX.XXX/XXXX-XX) in the database
- Listing pages pattern: client component with useCallback for loadData, useEffect to trigger, search resets to page 1
- Form dialogs: reuse same Dialog for create/edit, distinguish via `editingId` state (null = create, string = edit)
- Company context: `useCompany()` hook from `@/contexts/company-context` gives `selectedCompanyId`, `selectedCompany`, `companies`
- Company-scoped server actions should accept `companyId` param and validate access (not rely on client-side context alone)
- Shadcn DropdownMenu available in `src/components/ui/dropdown-menu.tsx`
- RBAC helpers in `src/lib/rbac.ts`: `canAccessCompany()`, `canAccessModule()`, `requireCompanyAccess()`, `requireModuleAccess()` — use in company-scoped server actions
- Admin role bypasses all RBAC checks; Manager role requires UserCompany assignment and module permissions
- For company-scoped server actions, use `requireCompanyAccess(companyId)` instead of `requireSession()` — it validates both auth and company access
- User server actions in `src/app/(app)/configuracoes/usuarios/actions.ts` — exports `createUser`, `updateUser`, `listUsers`, `getUserById`, `assignUserToCompanies`, `toggleUserStatus`
- Always use explicit `select` on User queries to exclude `passwordHash` from responses
- `assignUserToCompanies` replaces all assignments atomically — pass empty array to remove all
- Shadcn Checkbox in `src/components/ui/checkbox.tsx`, Select in `src/components/ui/select.tsx` — available for forms
- AVAILABLE_MODULES for RBAC UI: "comercial", "sac", "financeiro", "fiscal", "configuracoes"
- Two-dialog pattern: separate create/edit dialog from assignment/config dialog for complex entities
- Audit logging: `logAuditEvent()` from `@/lib/audit` — fire-and-forget, never throws. Use `Prisma.InputJsonValue` for dataBefore/dataAfter types
- Logout API at POST `/api/auth/logout` — clears both cookies, logs LOGOUT audit event
- Audit log server actions in `src/app/(app)/configuracoes/auditoria/actions.ts` — `listAuditLogs`, `exportAuditLogsCsv`, `getAuditEntityTypes`, `getAuditUsers`, `getAuditCompanies`
- CSV export pattern: generate CSV string server-side, create Blob with BOM (`\uFEFF`) client-side for proper Excel encoding, trigger download via temporary `<a>` element
- Filter dropdowns: use `__all__` as sentinel value for "all items" since Radix Select doesn't support empty string values
- Dashboard data in `src/app/(app)/dashboard/actions.ts` — uses placeholder KPIs until financial tables exist; replace `generatePlaceholderKPI` with real queries after US-017+
- Currency formatting: use `Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" })` for BRL values
- Recharts installed for charts — use `ResponsiveContainer` + `BarChart`/`LineChart` for responsive charts; wrap in a fixed-height div
- Chart currency formatting: use abbreviated format (R$ 10k, R$ 1.5M) for Y-axis labels via custom `tickFormatter`
- Email service in `src/lib/email.ts`: `sendEmail(options)` with `EmailProvider` interface; uses ConsoleEmailProvider in dev (logs to console)
- Password reset actions in `src/lib/password-reset.ts`: `requestPasswordReset(email)`, `validateResetToken(token)`, `resetPassword(token, newPassword)`
- Public auth pages (`/forgot-password`, `/reset-password`) live under `src/app/` (not `(app)` group) — update `PUBLIC_PATHS` in middleware when adding new public routes
- Session inactivity timeout: 30 min sliding window — middleware re-sets accessToken cookie maxAge on every request; JWT expiry also 30 min
- PasswordResetToken model stores reset tokens with 1-hour expiry and `usedAt` field for single-use enforcement
- Financial enums: `ClientType` (PF/PJ), `PaymentStatus` (PENDING/PAID/OVERDUE), `FinancialCategoryType` (INCOME/EXPENSE), `Recurrence` (NONE/WEEKLY/MONTHLY/YEARLY)
- Monetary values use `Decimal @db.Decimal(12, 2)` — never use Float for money
- Client cpfCnpj is unique per company: `@@unique([cpfCnpj, companyId])` — not globally unique
- Client model is used by both financial (receivables) and CRM (proposals, SAC) modules
- Prisma Decimal values must be serialized to strings for server action transfer — `.toString()` server-side, `parseFloat()` client-side
- Financial pages go under `src/app/(app)/financeiro/` — use `requireCompanyAccess(companyId)` for auth
- Use `Prisma.Decimal` constructor for creating monetary values: `new Prisma.Decimal(numericValue)`
- Payable server actions in `src/app/(app)/financeiro/pagar/actions.ts` — `listPayables`, `createPayable`, `updatePayable`, `markPayableAsPaid`, `listCategoriesForSelect`
- Payable supplier is a free-text field — use `contains` with `mode: "insensitive"` for supplier search filter
- Cash flow aggregation: use `Number(decimal)` for Prisma Decimal → number conversion in summation loops; `paidAt` for realized, `dueDate` for projections
- Period grouping keys: generate all keys between date range to fill chart gaps; sort lexicographically (YYYY-MM-DD format)
- `logAuditEvent` takes a single params object: `logAuditEvent({ userId, action, entity, entityId, dataBefore?, dataAfter?, ipAddress?, companyId? })`
- BankTransaction model: date, description, value (Decimal), status (PENDING/RECONCILED), matchedType, matchedEntityId, companyId — used for bank reconciliation
- File upload pattern: hidden `<input type="file" ref={...}>` triggered by button, read content with `file.text()`, pass string to server action
- Bank reconciliation matching: auto-match by value (exact, within 0.01) and date proximity (5 days); positive bank txns match receivables, negative match payables
- Shadcn Tabs component in `src/components/ui/tabs.tsx`, Badge in `src/components/ui/badge.tsx` — available for UI
- Multi-tab page pattern: split page into tab-specific components with shared state lifted to parent; use `onDataChanged` callback for cross-tab refresh
- PDF export: `jspdf` + `jspdf-autotable` — client-side generation; use `autoTable(doc, {...})` with `didParseCell` for styling
- Excel export: `xlsx` (SheetJS) — client-side; `XLSX.utils.aoa_to_sheet()` for data, `XLSX.writeFile(wb, name)` for download
- SAC ticket actions in `src/app/(app)/sac/tickets/actions.ts` — `listTickets`, `createTicket`, `listClientsForSelect`, `listUsersForAssign`
- SAC pages under `src/app/(app)/sac/` — `/sac` redirects to `/sac/tickets`; ticket listing at `/sac/tickets`
- User→UserCompany relation name is `userCompanies` (not `companies`) — use `userCompanies: { some: { companyId } }` for filtering users with company access
- Shadcn Textarea component in `src/components/ui/textarea.tsx` — available for multi-line text input
- Detail pages pattern: create `[id]/page.tsx` under the listing page dir; use `useParams()` for route params, `useRouter()` for back navigation
- Status transitions: define `VALID_STATUS_TRANSITIONS` map server-side and `STATUS_TRANSITIONS` (with labels) client-side for controlled state machines
- Ticket detail actions in `src/app/(app)/sac/tickets/actions.ts`: `getTicketById`, `updateTicketStatus`, `reassignTicket`
- Ticket listing rows are clickable — navigate to `/sac/tickets/[id]` detail page
- ClientTimeline reusable component in `src/components/client-timeline.tsx` — accepts `clientId` and `companyId` props; aggregates tickets, receivables (boletos), and email messages into a unified chronological timeline with filter-by-type dropdown
- Client detail actions in `src/app/(app)/comercial/clientes/[id]/actions.ts` — `getClientById`, `getClientTimeline` (unified timeline data fetcher)
- Comercial pages go under `src/app/(app)/comercial/` — client detail page at `/comercial/clientes/[id]`
- Ticket message server actions in `src/app/(app)/sac/tickets/actions.ts`: `listTicketMessages`, `createTicketReply` — messages ordered chronologically (ASC) for chat-style display
- `createTicketReply` optionally sends email to client via `sendEmail()` from `@/lib/email`; email failure is caught silently so the reply is still saved
- CPF validation utility in `src/lib/cpf.ts`: `isValidCpf()`, `stripCpf()`, `formatCpf()` — validates check digits, mirrors CNPJ utility pattern
- Client CRM server actions in `src/app/(app)/comercial/clientes/actions.ts` — `listClients`, `createClient`, `updateClient`, `getClientForEdit`; supports PF/PJ with CPF/CNPJ validation
- `/comercial` redirects to `/comercial/clientes` (same pattern as `/sac` → `/sac/tickets`)
- Client listing uses CPF/CNPJ mask helpers (`applyCpfMask`, `applyCnpjMask`); type selector clears CPF/CNPJ field on change to avoid invalid state
- Proposal/Boleto enums: `ProposalStatus` (DRAFT/SENT/ACCEPTED/REJECTED/EXPIRED), `BoletoStatus` (GENERATED/SENT/PAID/OVERDUE/CANCELLED)
- Ticket→Proposal and Ticket→Boleto use named relations ("TicketProposal", "TicketBoleto") with `onDelete: SetNull`
- Proposal.totalValue is stored but should be computed from items in server actions; ProposalItem.quantity is Decimal(10,2) for fractional quantities
- Proposal server actions in `src/app/(app)/comercial/propostas/actions.ts` — `listProposals`, `createProposal`, `updateProposal`, `getProposalById`, `updateProposalStatus`, `listClientsForProposal`
- Proposal status transitions: DRAFT→SENT, SENT→ACCEPTED/REJECTED/EXPIRED — terminal states (ACCEPTED/REJECTED/EXPIRED) have no further transitions
- Only DRAFT proposals can be edited; updateProposal replaces all items atomically in a $transaction
- Boleto service in `src/lib/boleto.ts`: abstract `BoletoProvider` interface with `MockBoletoProvider`; follows same provider pattern as email service
- Boleto server actions in `src/app/(app)/comercial/propostas/actions.ts`: `generateBoletosForProposal`, `listBoletosForProposal`, `updateBoletoStatus`
- Boleto status transitions: GENERATED→SENT/CANCELLED, SENT→PAID/OVERDUE/CANCELLED, OVERDUE→PAID/CANCELLED; PAID and CANCELLED are terminal
- Proposal detail page at `/comercial/propostas/[id]` — shows proposal info, items, and boletos; non-DRAFT proposals navigate here from listing
- Boleto status badge colors: gray=GENERATED, blue=SENT, green=PAID, red=OVERDUE, orange=CANCELLED
- Email template system in `src/lib/email-templates.ts`: `buildProposalEmailHtml(branding, data)`, `buildBoletoEmailHtml(branding, data)` — generates branded HTML emails with company header/footer
- Email server actions in `src/lib/email-actions.ts`: `sendProposalEmail(proposalId, companyId)`, `sendBoletoEmail(boletoId, companyId)` — sends emails with attachments, updates status, logs audit events

## Completed Stories

## 2026-02-28 - US-001
- What was implemented: Full project scaffold with Next.js 14, TypeScript, TailwindCSS, Shadcn/UI, and Prisma
- Files changed:
  - erp/ — entire project directory created
  - erp/package.json — Next.js 14 with all dependencies
  - erp/src/components/ui/ — Button, Input, Card, Table, Dialog, Sonner
  - erp/src/lib/prisma.ts — Prisma client singleton
  - erp/src/lib/utils.ts — Shadcn/UI utility (cn)
  - erp/prisma/schema.prisma — Base Prisma schema with PostgreSQL
  - erp/.env.example — Database URL template
- **Learnings for future iterations:**
  - `create-next-app@14` is the correct command for Next.js 14 scaffold
  - Shadcn/UI uses `sonner` for toasts now (not `toaster` — that component was removed from registry)
  - Prisma 6.x generates `prisma.config.ts` automatically — keep it for config
  - Node 18 has engine warnings but everything works fine
  - The `scripts/ralph/` directory is an embedded git repo — don't try to stage it
---

## 2026-02-28 - US-002
- What was implemented: Full Prisma schema for auth and multi-tenancy with Company, User, UserCompany, AuditLog models
- Files changed:
  - erp/prisma/schema.prisma — Added Company, User, UserCompany, AuditLog models with enums (UserRole, Status)
  - erp/prisma/migrations/20260228221218_init_auth_multitenancy/migration.sql — PostgreSQL migration
  - erp/prisma/seed.ts — Seed script creating 2 companies (Mendes Tech, Mendes Consultoria) and 1 admin user
  - erp/package.json — Added prisma seed config and tsx dev dependency
- **Learnings for future iterations:**
  - Prisma 6.19 does NOT support `seed` in prisma.config.ts — use package.json `"prisma"` field instead
  - PostgreSQL not pre-installed in devcontainer — install with `sudo apt-get install postgresql` and configure via `sudo su -c "..." postgres`
  - Use `@@map("table_name")` in Prisma for snake_case table names while keeping camelCase in code
  - UserCompany uses composite `@@id([userId, companyId])` for the junction table
  - `modules` field is `String[]` (PostgreSQL array) for flexible module permission storage
  - Seed admin password hash is for "admin123" — future auth stories should use bcrypt to hash
---

## 2026-02-28 - US-003
- What was implemented: JWT authentication utilities and middleware
  - `src/lib/auth.ts`: token generation (access 15min, refresh 7d), verification, password hashing/comparison (bcrypt), getCurrentUser helper
  - `src/lib/auth-middleware.ts`: `withAuth` higher-order function that wraps API route handlers, validates Bearer token, passes decoded JwtPayload
  - `src/app/api/auth/refresh/route.ts`: POST endpoint that reads refresh token from httpOnly cookie, validates, re-issues new access + refresh tokens
  - `.env.example`: added JWT_SECRET and REFRESH_SECRET template vars
- Files changed:
  - erp/src/lib/auth.ts (new)
  - erp/src/lib/auth-middleware.ts (new)
  - erp/src/app/api/auth/refresh/route.ts (new)
  - erp/.env.example (updated)
  - erp/package.json, erp/package-lock.json (added jsonwebtoken, bcryptjs + types)
- **Learnings for future iterations:**
  - Auth module exports: `hashPassword`, `comparePassword`, `generateAccessToken`, `generateRefreshToken`, `verifyAccessToken`, `verifyRefreshToken`, `getCurrentUser`
  - `withAuth` middleware pattern: wrap handler `(req, context, user) => ...` — user is the decoded JwtPayload
  - Refresh token is stored in httpOnly cookie named "refreshToken" — set via `response.cookies.set()`
  - Next.js 14 App Router route handlers that don't use `req` should omit the parameter entirely (not `_req`) to satisfy ESLint
  - JWT secrets fallback to dev defaults — always override in production via env vars
---

## 2026-02-28 - US-004
- What was implemented: Login page UI with full auth flow
  - `src/app/login/page.tsx`: Client component with email/password form, client-side validation (required fields, email format), error display, loading state
  - `src/app/api/auth/login/route.ts`: POST endpoint that validates credentials against DB, returns user info, sets accessToken and refreshToken cookies
  - `src/middleware.ts`: Next.js middleware that redirects unauthenticated users to /login, allows public paths (/login, /api/auth/*)
  - `src/app/dashboard/page.tsx`: Placeholder dashboard page (redirect target after login)
  - `src/components/ui/label.tsx`: Shadcn-style Label component for form labels
  - `src/app/api/auth/refresh/route.ts`: Updated to also set accessToken cookie on refresh
- Files changed:
  - erp/src/app/login/page.tsx (new)
  - erp/src/app/api/auth/login/route.ts (new)
  - erp/src/middleware.ts (new)
  - erp/src/app/dashboard/page.tsx (new)
  - erp/src/components/ui/label.tsx (new)
  - erp/src/app/api/auth/refresh/route.ts (updated)
- **Learnings for future iterations:**
  - Access token is stored in a non-httpOnly cookie so middleware can check auth state for redirects
  - `jsonwebtoken` doesn't work in Edge runtime — middleware just checks cookie existence, actual verification happens in API routes
  - Next.js middleware matcher pattern `/((?!_next/static|_next/image|favicon.ico).*)` excludes static files
  - Login form uses Portuguese labels and error messages (pt-BR locale)
  - The `PUBLIC_PATHS` array in middleware should be updated when adding new public routes
  - Browser testing not available — manual verification needed
---

## 2026-02-28 - US-005
- What was implemented: Base app layout with collapsible sidebar and header
  - `src/app/(app)/layout.tsx`: Client component with sidebar + header + mobile overlay, manages collapsed/mobileOpen state
  - `src/components/sidebar.tsx`: Fixed sidebar with nav links (Dashboard, Comercial, SAC, Financeiro, Fiscal, Configurações), collapsible with chevron toggle, active state highlighting
  - `src/components/header.tsx`: Fixed header with mobile hamburger menu, company selector placeholder, user avatar/name display
  - `src/app/(app)/dashboard/page.tsx`: Dashboard moved under (app) route group
  - `src/app/page.tsx`: Root redirects to /dashboard
  - Deleted old `src/app/dashboard/page.tsx`
- Files changed:
  - erp/src/app/(app)/layout.tsx (new)
  - erp/src/app/(app)/dashboard/page.tsx (new — moved from app/dashboard/)
  - erp/src/components/sidebar.tsx (new)
  - erp/src/components/header.tsx (new)
  - erp/src/app/page.tsx (updated — redirect to /dashboard)
  - erp/src/app/dashboard/page.tsx (deleted)
- **Learnings for future iterations:**
  - `(app)` route group wraps all authenticated pages — new pages go under `src/app/(app)/`
  - Sidebar navigation items are in `navItems` array in `sidebar.tsx` — add new routes there
  - Header uses responsive classes: hamburger visible on mobile (`md:hidden`), sidebar left offset on desktop
  - Mobile sidebar uses `translate-x` transform for slide animation with overlay backdrop
  - `lucide-react` is already installed for icons — use it for all icon needs
  - Browser testing not available — manual verification needed
---

## 2026-02-28 - US-006
- What was implemented: Company CRUD server actions with CNPJ validation and admin role enforcement
  - `src/lib/cnpj.ts`: CNPJ validation utility with check digit validation, strip, and format functions
  - `src/lib/session.ts`: Server-action auth helpers (`getSession`, `requireSession`, `requireAdmin`) using cookie-based auth
  - `src/app/(app)/configuracoes/empresas/actions.ts`: Five server actions — createCompany, updateCompany, listCompanies (paginated + search), getCompanyById, toggleCompanyStatus
- Files changed:
  - erp/src/lib/cnpj.ts (new)
  - erp/src/lib/session.ts (new)
  - erp/src/app/(app)/configuracoes/empresas/actions.ts (new)
- **Learnings for future iterations:**
  - Server actions need cookie-based auth (not Bearer token) — `cookies()` from `next/headers` is the way
  - `requireAdmin()` pattern: call at the top of every admin-only server action for consistent authorization
  - CNPJ validation includes check digit math — don't just check format length
  - `PaginatedResult<T>` generic type provides consistent pagination shape for all listing actions
  - CNPJs stored formatted in DB for consistent comparison and display
  - `mode: "insensitive"` on Prisma `contains` enables case-insensitive search
---

## 2026-02-28 - US-007
- What was implemented: Company registration and listing page with full CRUD UI
  - `src/app/(app)/configuracoes/empresas/page.tsx`: Client component with company listing table, search, pagination, create/edit dialog, CNPJ input mask, and active/inactive toggle
- Files changed:
  - erp/src/app/(app)/configuracoes/empresas/page.tsx (new)
- **Learnings for future iterations:**
  - CNPJ mask is implemented as a pure function `applyCnpjMask()` — can be extracted to a shared utility if needed elsewhere
  - Inline CNPJ validation triggers when all 14 digits are typed, using the existing `isValidCnpj()` from `@/lib/cnpj`
  - Status toggle is a clickable badge (green for ACTIVE, gray for INACTIVE) — no separate button needed
  - `PaginatedResult<T>` type is imported from the actions file for consistent typing
  - Dialog uses `max-h-[90vh] overflow-y-auto` to handle many form fields on small screens
  - Search resets pagination to page 1 via `handleSearchChange`
  - Server actions from `actions.ts` are called directly from client components — Next.js handles the RPC
  - Browser testing not available — manual verification needed
---

## 2026-02-28 - US-008
- What was implemented: Company selector component in header with React context
  - `src/lib/company-actions.ts`: Server action `getUserCompanies()` — Admin sees all active companies, Manager sees only assigned companies via UserCompany junction
  - `src/contexts/company-context.tsx`: `CompanyProvider` + `useCompany()` hook — stores selected company in React context, persists to localStorage (`mendes-erp-selected-company`), auto-restores on session load
  - `src/components/header.tsx`: Updated with DropdownMenu-based company selector — shows company name + CNPJ, check icon for selected, loading and empty states
  - `src/app/(app)/layout.tsx`: Wrapped with `CompanyProvider` so all pages can access `useCompany()`
  - `src/components/ui/dropdown-menu.tsx`: Shadcn DropdownMenu component installed
- Files changed:
  - erp/src/lib/company-actions.ts (new)
  - erp/src/contexts/company-context.tsx (new)
  - erp/src/components/ui/dropdown-menu.tsx (new — Shadcn)
  - erp/src/components/header.tsx (updated)
  - erp/src/app/(app)/layout.tsx (updated)
  - erp/package.json, erp/package-lock.json (added @radix-ui/react-dropdown-menu)
- **Learnings for future iterations:**
  - `CompanyProvider` wraps the `(app)` layout — use `useCompany()` hook in any client component under `(app)` to get `selectedCompanyId`, `selectedCompany`, `companies`
  - `getUserCompanies()` server action in `src/lib/company-actions.ts` handles role-based filtering (Admin vs Manager)
  - localStorage key `mendes-erp-selected-company` stores the selected company ID — auto-restored on page load
  - DropdownMenu from Shadcn uses `@radix-ui/react-dropdown-menu` — already installed
  - When creating new pages that need company scope, access `selectedCompanyId` from `useCompany()` hook
  - Browser testing not available — manual verification needed
---

## 2026-02-28 - US-009
- What was implemented: Role-based access control helpers and middleware
  - `src/lib/rbac.ts`: Four functions — `canAccessCompany(userId, role, companyId)`, `canAccessModule(userId, role, companyId, module)`, `requireCompanyAccess(companyId)`, `requireModuleAccess(companyId, module)`
  - Admin role bypasses all company-level and module-level checks
  - Manager role restricted to assigned companies (via UserCompany junction) and assigned modules (via UserCompany.modules array)
  - `requireCompanyAccess` and `requireModuleAccess` are server action middleware — call at the top of any company-scoped action
  - Existing server actions verified: company CRUD uses `requireAdmin()` (correct for admin-only ops), `getUserCompanies` already filters by role
- Files changed:
  - erp/src/lib/rbac.ts (new)
- **Learnings for future iterations:**
  - Prisma composite key `userId_companyId` is used for UserCompany lookups — use `findUnique` with `{ userId_companyId: { userId, companyId } }`
  - If UserCompany.modules is empty array, it means the user has access to all modules for that company (no restrictions)
  - Use `requireCompanyAccess(companyId)` in future company-scoped server actions (e.g., financeiro, SAC) instead of plain `requireSession()`
  - Company CRUD is admin-only — no need for company-scoped RBAC on those actions
  - No UI changes in this story — purely backend utilities
---

## 2026-02-28 - US-010
- What was implemented: User management server actions (CRUD + company assignments)
  - `src/app/(app)/configuracoes/usuarios/actions.ts`: Six server actions:
    - `createUser`: Creates user with name, email, password, role; hashes password with bcrypt; validates email uniqueness
    - `updateUser`: Updates user fields; optionally updates password if provided
    - `listUsers`: Paginated user listing with search by name or email
    - `getUserById`: Get single user with company assignments included
    - `assignUserToCompanies`: Replaces all UserCompany assignments in a transaction; validates company existence
    - `toggleUserStatus`: Toggles between ACTIVE/INACTIVE
- Files changed:
  - erp/src/app/(app)/configuracoes/usuarios/actions.ts (new)
- **Learnings for future iterations:**
  - User actions follow the same pattern as company actions — `requireAdmin()` at top, input validation helper, paginated listing
  - `assignUserToCompanies` uses `prisma.$transaction` to delete-then-create for atomic replacement of assignments
  - Email is normalized to lowercase before storage and uniqueness checks
  - Password is optional on update (only hashed/updated if provided) — use `undefined` check, not truthiness
  - User select omits `passwordHash` from all responses — always use explicit `select` to avoid leaking sensitive data
  - `CompanyAssignment` type exported for UI consumption: `{ companyId: string; modules: string[] }`
---

## 2026-02-28 - US-011
- What was implemented: User management page with full CRUD UI and company assignment dialog
  - `src/app/(app)/configuracoes/usuarios/page.tsx`: Client component with:
    - User listing table with columns: name, email, role (with badge), status (toggle)
    - Search by name or email with pagination
    - Create user dialog: name, email, password, role (Admin/Manager via Select component)
    - Edit user dialog with pre-populated data (password optional on edit)
    - Company assignment dialog: multi-select of companies with module checkboxes per company
    - Status toggle (active/inactive) per user
  - `src/app/(app)/configuracoes/usuarios/actions.ts`: Added `listAllCompanies()` server action for company assignment UI
  - `src/components/ui/checkbox.tsx`: Shadcn Checkbox component installed
  - `src/components/ui/select.tsx`: Shadcn Select component installed
- Files changed:
  - erp/src/app/(app)/configuracoes/usuarios/page.tsx (new)
  - erp/src/app/(app)/configuracoes/usuarios/actions.ts (updated — added listAllCompanies)
  - erp/src/components/ui/checkbox.tsx (new — Shadcn)
  - erp/src/components/ui/select.tsx (new — Shadcn)
  - erp/package.json, erp/package-lock.json (added @radix-ui/react-checkbox, @radix-ui/react-select)
- **Learnings for future iterations:**
  - User page uses two separate dialogs: one for create/edit user, one for company assignment — keeps UI simpler than one giant form
  - Company assignment dialog loads all companies and current assignments in parallel via `Promise.all`
  - `listAllCompanies()` server action returns only active companies for the assignment UI
  - AVAILABLE_MODULES constant defines the 5 business modules: comercial, sac, financeiro, fiscal, configuracoes
  - Shadcn Select component requires `@radix-ui/react-select` — use for dropdowns with fixed options (like role)
  - Shadcn Checkbox component requires `@radix-ui/react-checkbox` — use for boolean toggles in forms
  - Browser testing not available — manual verification needed
---

## 2026-02-28 - US-012
- What was implemented: Audit logging service with utility function and integration across all existing server actions
  - `src/lib/audit.ts`: `logAuditEvent()` utility — fire-and-forget (catches errors to avoid disrupting main operations), insert-only, supports action types: CREATE, UPDATE, DELETE, LOGIN, LOGOUT, STATUS_CHANGE
  - Company CRUD actions updated: createCompany (CREATE), updateCompany (UPDATE with dataBefore/dataAfter), toggleCompanyStatus (STATUS_CHANGE)
  - User CRUD actions updated: createUser (CREATE), updateUser (UPDATE with dataBefore/dataAfter), assignUserToCompanies (UPDATE), toggleUserStatus (STATUS_CHANGE)
  - Login route updated: logs LOGIN event with IP address extraction from x-forwarded-for/x-real-ip headers
  - `src/app/api/auth/logout/route.ts` (new): POST endpoint that clears both cookies, logs LOGOUT event, extracts IP address
- Files changed:
  - erp/src/lib/audit.ts (new)
  - erp/src/app/api/auth/logout/route.ts (new)
  - erp/src/app/(app)/configuracoes/empresas/actions.ts (updated — added audit logging)
  - erp/src/app/(app)/configuracoes/usuarios/actions.ts (updated — added audit logging)
  - erp/src/app/api/auth/login/route.ts (updated — added audit logging)
- **Learnings for future iterations:**
  - Prisma `Json` fields require `Prisma.InputJsonValue` type — `Record<string, unknown>` is NOT compatible. Use `as unknown as Prisma.InputJsonValue` for type casts
  - `requireAdmin()` returns the session JwtPayload — capture the return value (`const session = await requireAdmin()`) to use `session.userId` for audit logging
  - IP extraction pattern: `req.headers.get("x-forwarded-for")?.split(",")[0]?.trim() ?? req.headers.get("x-real-ip")` — works behind proxies
  - Audit logging is fire-and-forget: wrapped in try/catch with console.error fallback, so failures never block the main operation
  - No UI changes in this story — purely backend service
---

## 2026-03-01 - US-013
- What was implemented: Audit log viewer page with filters, pagination, and CSV export
  - `src/app/(app)/configuracoes/auditoria/actions.ts`: Five server actions:
    - `listAuditLogs`: Paginated listing with filters (user, action, entity, company, date range), includes joined user name and company name
    - `exportAuditLogsCsv`: Generates CSV string with same filters (up to 10,000 records), includes BOM for Excel compatibility
    - `getAuditEntityTypes`: Returns distinct entity types for filter dropdown
    - `getAuditUsers`: Returns all users for filter dropdown
    - `getAuditCompanies`: Returns all companies for filter dropdown
  - `src/app/(app)/configuracoes/auditoria/page.tsx`: Client component with:
    - Paginated table with columns: timestamp, user, action (color-coded badge), entity (with truncated ID), company
    - Collapsible filter panel with 6 filters: user, action type, entity type, company, date from, date to
    - CSV export button that downloads filtered results
    - Chronological ordering (most recent first)
- Files changed:
  - erp/src/app/(app)/configuracoes/auditoria/actions.ts (new)
  - erp/src/app/(app)/configuracoes/auditoria/page.tsx (new)
- **Learnings for future iterations:**
  - Radix Select doesn't support empty string values — use a sentinel like `__all__` and map back to empty string in the change handler
  - CSV export with BOM (`\uFEFF` prefix) ensures Excel opens the file with correct encoding for Portuguese characters
  - Prisma `distinct` option on `findMany` is useful for getting unique values (e.g., distinct entity types)
  - Filter panels: toggle visibility with state, show indicator badge when filters are active
  - Date range filtering: use `gte`/`lte` on `createdAt`, remember to set end date to 23:59:59.999 for inclusive end of day
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-014
- What was implemented: Consolidated dashboard with KPI cards and per-company breakdown
  - `src/app/(app)/dashboard/actions.ts`: Server action `getDashboardData()` — fetches companies based on user role (Admin=all, Manager=assigned), generates placeholder KPI data (revenue, expenses, profit) scaled by period type
  - `src/app/(app)/dashboard/page.tsx`: Client component with:
    - 3 KPI cards: Receita Total (green), Despesas Totais (red), Lucro Total (blue)
    - Period filter dropdown: Hoje, Esta Semana, Este Mês, Este Ano, Personalizado (with custom date range inputs)
    - Per-company breakdown table with columns: Empresa, Receita, Despesas, Lucro, Margem (%)
    - Consolidated totals row when multiple companies exist
    - Loading skeleton animation for KPI cards
    - Empty state for no companies
    - Currency formatting with Intl.NumberFormat pt-BR
- Files changed:
  - erp/src/app/(app)/dashboard/actions.ts (new)
  - erp/src/app/(app)/dashboard/page.tsx (rewritten from placeholder)
- **Learnings for future iterations:**
  - Placeholder KPI data uses deterministic hash from companyId for stable, varied numbers across companies
  - Period scaling: day=1x, week=7x, month=30x, year=365x of base values
  - `Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" })` for proper Brazilian currency formatting
  - Custom date range filter: only trigger data load when both start and end dates are set
  - Dashboard server action uses `requireSession()` (not `requireAdmin()`) since managers also need dashboard access
  - KPI data is placeholder — will be replaced with real financial data once US-017+ are implemented
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-015
- What was implemented: Dashboard comparison charts with recharts and boleto summary cards
  - `src/app/(app)/dashboard/actions.ts`: Added `BoletoSummary` and `RevenueChartEntry` types, `generatePlaceholderBoletoSummary()` helper, updated `DashboardData` and `getDashboardData` to return chart data and boleto summary
  - `src/app/(app)/dashboard/page.tsx`: Added:
    - Bar chart comparing revenue, expenses, and profit across companies using recharts `BarChart` + `ResponsiveContainer`
    - 3 boleto summary cards: Emitidos (gray), Pagos (green), Vencidos (red) with placeholder counts
    - Custom chart tooltip with formatted BRL currency
    - Abbreviated Y-axis labels (R$ 10k, R$ 1.5M) for readability
    - Charts respect the period filter from the dashboard
    - Responsive layout: chart container uses `h-[350px]` with 100% width
  - `erp/package.json`: Added `recharts` dependency
- Files changed:
  - erp/src/app/(app)/dashboard/actions.ts (updated — added chart + boleto types and data)
  - erp/src/app/(app)/dashboard/page.tsx (updated — added charts and boleto summary cards)
  - erp/package.json, erp/package-lock.json (added recharts)
- **Learnings for future iterations:**
  - Recharts requires wrapping charts in `ResponsiveContainer` with 100% width/height inside a fixed-height parent div
  - Use custom `tickFormatter` on YAxis for abbreviated currency (R$ 10k, R$ 1.5M)
  - When there are many companies, set XAxis `angle={-20}` and `textAnchor="end"` to avoid label overlap
  - Recharts Tooltip can accept a custom `content` component for styled tooltips
  - Placeholder boleto data scales by period type just like KPI data
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-016
- What was implemented: Password recovery flow with forgot/reset password pages, email service, and session expiry
  - `src/lib/email.ts`: Generic email sending interface with `EmailProvider` abstraction and `ConsoleEmailProvider` for development (logs to console)
  - `src/lib/password-reset.ts`: Three server actions — `requestPasswordReset` (generates token, sends email, avoids enumeration), `validateResetToken`, `resetPassword` (with token single-use enforcement)
  - `prisma/schema.prisma`: Added `PasswordResetToken` model with token, userId, expiresAt, usedAt fields
  - `src/app/forgot-password/page.tsx`: Email input form, success message after submission (always shows success to prevent enumeration)
  - `src/app/reset-password/page.tsx`: Token validation on load, new password + confirmation form, success/error states
  - `src/app/login/page.tsx`: Added "Esqueci minha senha" link to forgot-password page
  - `src/middleware.ts`: Added `/forgot-password` and `/reset-password` to PUBLIC_PATHS; added sliding session window (re-sets accessToken cookie maxAge=30min on each request)
  - `src/lib/auth.ts`: Changed ACCESS_TOKEN_EXPIRY from 15m to 30m
  - `src/app/api/auth/login/route.ts`: Updated cookie maxAge to 30 minutes
  - `src/app/api/auth/refresh/route.ts`: Updated cookie maxAge to 30 minutes
- Files changed:
  - erp/prisma/schema.prisma (updated — added PasswordResetToken model + User relation)
  - erp/prisma/migrations/20260301001127_add_password_reset_tokens/migration.sql (new)
  - erp/src/lib/email.ts (new)
  - erp/src/lib/password-reset.ts (new)
  - erp/src/app/forgot-password/page.tsx (new)
  - erp/src/app/reset-password/page.tsx (new)
  - erp/src/app/login/page.tsx (updated — added forgot password link)
  - erp/src/middleware.ts (updated — public paths + sliding session)
  - erp/src/lib/auth.ts (updated — 30m token expiry)
  - erp/src/app/api/auth/login/route.ts (updated — 30m cookie maxAge)
  - erp/src/app/api/auth/refresh/route.ts (updated — 30m cookie maxAge)
- **Learnings for future iterations:**
  - `crypto.randomBytes(32).toString("hex")` generates secure random tokens for password reset
  - Always return generic success response for password reset requests to avoid email enumeration attacks
  - `usedAt` field on tokens enables single-use enforcement — check `usedAt === null` before accepting token
  - Invalidate existing unused tokens before creating new ones to prevent token accumulation
  - Sliding session window: middleware re-sets cookie maxAge on every authenticated request, so cookie only expires after N min of inactivity
  - Public auth pages (`/forgot-password`, `/reset-password`) go under `src/app/` directly, not under `(app)` route group
  - `PUBLIC_PATHS` in middleware must be updated when adding new public routes
  - `$transaction` array syntax works for simple multi-operation atomicity (update user + mark token used)
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-017
- What was implemented: Financial database schema with Client, FinancialCategory, AccountReceivable, AccountPayable models
  - `prisma/schema.prisma`: Added 4 new enums (ClientType, PaymentStatus, FinancialCategoryType, Recurrence) and 4 new models
  - Client model: name, razaoSocial, cpfCnpj (unique per company via `@@unique([cpfCnpj, companyId])`), email, telefone, endereco, type (PF/PJ), scoped to company
  - FinancialCategory model: name, type (INCOME/EXPENSE), scoped to company
  - AccountReceivable model: clientId, description, value (Decimal 12,2), dueDate, status (PENDING/PAID/OVERDUE), paidAt, scoped to company
  - AccountPayable model: supplier, description, value (Decimal 12,2), dueDate, status (PENDING/PAID/OVERDUE), categoryId (optional), recurrence (NONE/WEEKLY/MONTHLY/YEARLY), paidAt, scoped to company
  - Migration `20260301001646_add_financial_schema` generated and applied
- Files changed:
  - erp/prisma/schema.prisma (updated — added 4 enums + 4 models + Company relations)
  - erp/prisma/migrations/20260301001646_add_financial_schema/migration.sql (new)
- **Learnings for future iterations:**
  - Use `Decimal @db.Decimal(12, 2)` for monetary values — avoids floating-point precision issues
  - `@@unique([cpfCnpj, companyId])` enforces per-tenant uniqueness for client CPF/CNPJ
  - categoryId on AccountPayable is optional (`String?`) with `onDelete: SetNull` — deleting a category doesn't cascade to payables
  - Client model is shared between financial (US-017) and CRM (US-031) — the full CRM UI is built later
  - PaymentStatus enum (PENDING/PAID/OVERDUE) is reused by both AccountReceivable and AccountPayable
  - No seed script needed for this story — financial data will be created via server actions in subsequent stories
---

## 2026-03-01 - US-018
- What was implemented: Accounts receivable server actions and listing page
  - `src/app/(app)/financeiro/receber/actions.ts`: Four server actions:
    - `listReceivables`: Paginated listing with filters (status, client, date range), includes joined client name, serializes Decimal values to string
    - `createReceivable`: Creates receivable with validation (client exists in company, value > 0), uses `Prisma.Decimal` for monetary value
    - `markReceivableAsPaid`: Manual write-off — sets status to PAID and paidAt to current date
    - `listClientsForSelect`: Returns clients for dropdown (id + name)
  - `src/app/(app)/financeiro/receber/page.tsx`: Client component with:
    - Paginated table with columns: client, description, value (BRL formatted), due date, status (color-coded badge), actions
    - Collapsible filter panel: status, client, date range (from/to)
    - Status badge colors: gray=pendente, green=pago, red=vencido
    - "Baixar" (write-off) button for non-paid receivables
    - Create receivable dialog: client selector, description, value, due date
    - Company-scoped via `useCompany()` context
- Files changed:
  - erp/src/app/(app)/financeiro/receber/actions.ts (new)
  - erp/src/app/(app)/financeiro/receber/page.tsx (new)
- **Learnings for future iterations:**
  - Prisma `Decimal` values must be serialized to strings for client-side transfer — use `.toString()` in server action, `parseFloat()` on client
  - Company-scoped server actions use `requireCompanyAccess(companyId)` from `@/lib/rbac` — returns session for audit logging
  - Use `Prisma.Decimal` constructor for creating monetary values: `new Prisma.Decimal(numericValue)`
  - Client select dropdown uses `listClientsForSelect()` — loads once on company change, reused in both filter and form
  - Filter panel pattern: toggle visibility, show indicator badge when active, "Limpar filtros" button
  - `__none__` sentinel for disabled placeholder in Select (Radix doesn't support empty/disabled default)
  - Financial pages go under `src/app/(app)/financeiro/` — the "Financeiro" sidebar link already points to `/financeiro`
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-019
- What was implemented: Accounts payable CRUD and listing page
  - `src/app/(app)/financeiro/pagar/actions.ts`: Five server actions:
    - `listPayables`: Paginated listing with filters (status, supplier text search, category, date range), includes joined category name, serializes Decimal values
    - `createPayable`: Creates payable with validation (supplier required, value > 0, optional category verified in company), supports recurrence setting
    - `updatePayable`: Updates payable fields with same validations, prevents editing paid items, logs dataBefore/dataAfter
    - `markPayableAsPaid`: Manual write-off — sets status to PAID and paidAt to current date
    - `listCategoriesForSelect`: Returns EXPENSE-type categories for dropdown
  - `src/app/(app)/financeiro/pagar/page.tsx`: Client component with:
    - Paginated table with columns: supplier, value (BRL formatted), due date, category, status (color-coded badge), actions
    - Collapsible filter panel: status, supplier (text search), category, date range (from/to)
    - Status badge colors: gray=pendente, green=pago, red=vencido
    - Create/edit dialog with fields: supplier, description, value, due date, category (optional dropdown), recurrence (dropdown)
    - Edit button (pencil icon) for non-paid payables
    - "Baixar" (write-off) button for non-paid payables
    - Company-scoped via `useCompany()` context
- Files changed:
  - erp/src/app/(app)/financeiro/pagar/actions.ts (new)
  - erp/src/app/(app)/financeiro/pagar/page.tsx (new)
- **Learnings for future iterations:**
  - Payable supplier is a free-text field (not FK) — use text search (`contains`, case-insensitive) for filtering
  - `listCategoriesForSelect` filters by `type: "EXPENSE"` — payables only show expense categories
  - `updatePayable` prevents editing paid items — check `existing.status === "PAID"` before update
  - Create/edit dialog reuses same Dialog component, distinguished by `editingId` state (null = create, string = edit)
  - Form grid layout uses `sm:grid-cols-2` for value/date and category/recurrence pairs
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-020
- What was implemented: Accounts payable due date alerts with auto-update, summary banner, and row highlighting
  - `src/app/(app)/financeiro/pagar/actions.ts`: Added `getPayableAlerts(companyId)` server action that:
    - Auto-updates PENDING payables with past due dates to OVERDUE status (`updateMany`)
    - Returns counts: `dueThisWeek` (PENDING items due within 7 days) and `overdue` (total OVERDUE items)
  - `src/app/(app)/financeiro/pagar/page.tsx`: Updated with:
    - Alert summary banner at top: red badge for overdue count, yellow badge for due-this-week count
    - Row highlighting: `bg-red-50` for overdue rows, `bg-yellow-50` for rows due within 7 days
    - `getPayableAlerts` called before `listPayables` on each load to ensure statuses are up-to-date
    - Added `AlertTriangle` and `Clock` icons from lucide-react
  - Exported `PayableAlertSummary` type for potential reuse
- Files changed:
  - erp/src/app/(app)/financeiro/pagar/actions.ts (updated — added getPayableAlerts + PayableAlertSummary type)
  - erp/src/app/(app)/financeiro/pagar/page.tsx (updated — added alert banner, row highlighting, alerts state)
- **Learnings for future iterations:**
  - `updateMany` is the efficient way to bulk-update statuses — no need to loop individual records
  - Alert summary pattern: fetch alerts before listing data so auto-update runs first and listing reflects updated statuses
  - Row highlighting uses `className` on `TableRow` — Shadcn Table supports arbitrary className props
  - Client-side date comparison for row highlighting: construct `startOfToday` using year/month/date to strip time, compare due dates
  - Plural/singular in Portuguese: use ternary for "conta vencida" (1) vs "contas vencidas" (>1)
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-021
- What was implemented: Cash flow page with income/expense chart and projections
  - `src/app/(app)/financeiro/fluxo-de-caixa/actions.ts`: Two server actions:
    - `getCashFlowData`: Aggregates paid receivables (income) and paid payables (expenses) by period, includes pending items for projections, supports daily/weekly/monthly grouping, computes current balance (realized) and projected balance (includes pending)
    - `getCompaniesForCashFlow`: Returns companies user can access for the consolidated view toggle
  - `src/app/(app)/financeiro/fluxo-de-caixa/page.tsx`: Client component with:
    - 4 KPI cards: Saldo Atual (realized balance), Saldo Projetado (includes pending), Total Receitas, Total Despesas
    - Bar chart comparing income vs expenses per period using recharts BarChart
    - Line chart showing net cash flow (projection) per period using recharts LineChart
    - Toggle between per-company and consolidated (holding) view
    - Period grouping selector: daily, weekly, monthly
    - Date range selector with default of current month through 3 months ahead
    - Empty state when no data or no company selected
- Files changed:
  - erp/src/app/(app)/financeiro/fluxo-de-caixa/actions.ts (new)
  - erp/src/app/(app)/financeiro/fluxo-de-caixa/page.tsx (new)
- **Learnings for future iterations:**
  - Cash flow combines paid (realized) + pending (projected) data — use paidAt for realized income/expenses, dueDate for pending projections
  - Period grouping helper: generate all period keys between from/to dates to ensure no gaps in chart even if no data exists
  - Weekly grouping uses ISO week start (Monday) via getDay() adjustment
  - `Number(decimal)` converts Prisma Decimal to number for aggregation — acceptable for display-only aggregation
  - Consolidated view passes `companyId: undefined` to skip company filter in Prisma where clause
  - Default date range: current month start to 3 months ahead for meaningful projection view
  - Cash flow page route: `/financeiro/fluxo-de-caixa` — accessed via Financeiro sidebar link
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-022
- What was implemented: Bank reconciliation page with file import (OFX and CSV)
  - `prisma/schema.prisma`: Added `BankTransactionStatus` enum (PENDING/RECONCILED) and `BankTransaction` model with date, description, value (Decimal 12,2), status, companyId
  - `prisma/migrations/20260301003612_add_bank_transactions/migration.sql`: Migration for BankTransaction table
  - `src/app/(app)/financeiro/conciliacao/actions.ts`: Server actions:
    - `importBankTransactions`: Parses OFX or CSV file content, creates BankTransaction records, returns import result (imported/skipped/errors count)
    - `listBankTransactions`: Paginated listing with filters (status, date range, description search)
    - `deleteBankTransaction`: Delete individual pending transactions
    - OFX parser: Extracts STMTTRN blocks, supports SGML and XML tag formats, parses DTPOSTED dates
    - CSV parser: Auto-detects delimiter (`;` or `,`), supports Portuguese and English header names, falls back to positional parsing (date, description, value), handles Brazilian number format (1.234,56)
  - `src/app/(app)/financeiro/conciliacao/page.tsx`: Client component with:
    - File upload button accepting .ofx, .qfx, .csv, .txt formats
    - Import instructions card explaining supported formats
    - Transactions table with columns: date, description, value (color-coded green/red), status badge, actions
    - Collapsible filter panel: status, description search, date range
    - Pagination with total count
    - Delete button for pending transactions
    - Company-scoped via useCompany() context
- Files changed:
  - erp/prisma/schema.prisma (updated — added BankTransactionStatus enum + BankTransaction model + Company relation)
  - erp/prisma/migrations/20260301003612_add_bank_transactions/migration.sql (new)
  - erp/src/app/(app)/financeiro/conciliacao/actions.ts (new)
  - erp/src/app/(app)/financeiro/conciliacao/page.tsx (new)
- **Learnings for future iterations:**
  - `logAuditEvent` takes a single params object (not positional args) — use `logAuditEvent({ userId, action, entity, entityId, ... })`
  - OFX format: transactions in `<STMTTRN>` blocks, date in `DTPOSTED` (YYYYMMDD), amount in `TRNAMT`, description in `MEMO` or `NAME`
  - OFX uses SGML-style tags (no closing tags) — parser must handle both SGML (`<FIELD>value`) and XML (`<FIELD>value</FIELD>`) formats
  - Brazilian CSV bank statements commonly use `;` delimiter and `1.234,56` number format
  - CSV header detection: support both Portuguese (`data`, `descricao`, `valor`) and English (`date`, `description`, `value`) column names
  - File upload uses hidden `<input type="file">` triggered by button click via `useRef` — reset `.value = ""` after import to allow re-selecting same file
  - `file.text()` reads file content as string client-side — pass string to server action (no File object transfer needed)
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-023
- What was implemented: Bank reconciliation matching UI with auto-matching, manual matching, and reconciliation summary
- Files changed:
  - erp/prisma/schema.prisma — Added `matchedType` and `matchedEntityId` optional fields to BankTransaction model
  - erp/prisma/migrations/20260301004132_add_bank_transaction_matching_fields/migration.sql — Migration for new fields
  - erp/src/app/(app)/financeiro/conciliacao/actions.ts — Added server actions: `getReconciliationSummary`, `listUnmatchedSystemRecords`, `autoMatchTransactions`, `manualMatchTransaction`, `unmatchTransaction`
  - erp/src/app/(app)/financeiro/conciliacao/page.tsx — Rewrote with Tabs: "Conciliação" tab (matching view with summary, side-by-side, manual match dialog) and "Importação" tab (existing import)
  - erp/src/components/ui/tabs.tsx — Added Shadcn Tabs component
  - erp/src/components/ui/badge.tsx — Added Shadcn Badge component
- **Learnings for future iterations:**
  - Auto-matching logic: positive bank transactions match receivables, negative match payables (absolute value comparison)
  - Match by exact value (0.01 tolerance) and date proximity (5 days window) — pick closest date match
  - Use `matchedType` (string: "RECEIVABLE"/"PAYABLE") + `matchedEntityId` for flexible entity linking without foreign keys
  - Track claimed IDs during auto-match batch to prevent duplicate matching in same run
  - Side-by-side layout uses `grid-cols-1 lg:grid-cols-2` for responsive two-column view
  - Multi-tab page: split into sub-components (ImportTab, MatchingTab) with shared summary state in parent
  - Manual match dialog: show bank transaction details in Card, searchable system records list below
  - Unmatch action resets both status and matching fields (status→PENDING, matchedType→null, matchedEntityId→null)
  - Browser testing not available — manual verification needed
---

## 2026-03-01 - US-024
- What was implemented: DRE (Demonstração do Resultado do Exercício) income statement report page
- Files changed:
  - erp/src/app/(app)/financeiro/dre/actions.ts — Server actions for DRE data aggregation (getDREData, getDREConsolidated, getCompaniesForDRE)
  - erp/src/app/(app)/financeiro/dre/page.tsx — Full DRE page with income statement table, per-company/consolidated views, period filters
- **Learnings for future iterations:**
  - DRE uses PAID receivables for revenue and PAID payables grouped by category for expenses
  - Deductions and cost of services are placeholder (0) until fiscal module (US-042+) is built
  - Period filters use year + period number (month 1-12, quarter 1-4, or annual with no sub-period)
  - Consolidated view requires querying per-company then aggregating — use computeDREForCompany helper
  - CompanyComparisonTable component shows side-by-side comparison of all companies in consolidated view
  - DRE page follows same viewMode (company/consolidated) pattern as cash flow page
  - The DRE structure: Receita Bruta → Deduções → Receita Líquida → Custos → Lucro Bruto → Despesas Operacionais → Resultado Operacional
---

## 2026-03-01 - US-025
- What was implemented: PDF and Excel export buttons for the DRE (income statement) report
- Files changed:
  - erp/package.json — Added jspdf, jspdf-autotable, xlsx dependencies
  - erp/src/app/(app)/financeiro/dre/export.ts — New file with exportDREtoPDF and exportDREtoExcel functions
  - erp/src/app/(app)/financeiro/dre/page.tsx — Added export buttons (PDF/Excel) to header, import export functions
- **Learnings for future iterations:**
  - jspdf + jspdf-autotable works well for tabular PDF reports; use autoTable(doc, {...}) with didParseCell for row styling
  - xlsx (SheetJS) uses XLSX.utils.aoa_to_sheet for array-of-arrays → worksheet conversion, XLSX.writeFile to trigger download
  - Both PDF and Excel exports run client-side since data is already loaded in component state — no server action needed
  - For PDF column alignment, use columnStyles with halign: "right" for numeric columns
  - jspdf-autotable exposes lastAutoTable.finalY on the doc object for positioning subsequent content (needs any cast)
  - Export buttons should only show when data is loaded (!loading && data !== null)
---

## 2026-03-01 - US-026
- What was implemented: SAC database schema with Ticket and TicketMessage models
  - `TicketPriority` enum: HIGH, MEDIUM, LOW
  - `TicketStatus` enum: OPEN, IN_PROGRESS, WAITING_CLIENT, RESOLVED, CLOSED
  - `Ticket` model: id, clientId, subject, description, priority, status, assigneeId (nullable), proposalId (nullable), boletoId (nullable), companyId, createdAt, updatedAt
  - `TicketMessage` model: id, ticketId, senderId, content, sentViaEmail (boolean), createdAt
  - Relations: Ticket → Client, Ticket → User (assignee), Ticket → Company, TicketMessage → Ticket, TicketMessage → User (sender)
- Files changed:
  - erp/prisma/schema.prisma — Added TicketPriority, TicketStatus enums; Ticket, TicketMessage models; reverse relations on Company, User, Client
  - erp/prisma/migrations/20260301005810_add_sac_schema/migration.sql — PostgreSQL migration
- **Learnings for future iterations:**
  - Ticket model uses named relations for User: `@relation("TicketAssignee")` for assignee and `@relation("TicketMessageSender")` for message sender — needed because both reference User
  - proposalId and boletoId are plain String? fields (not FK relations) since Proposal/Boleto models don't exist yet (US-033)
  - SAC pages go under `src/app/(app)/sac/` — follow same pattern as financeiro module
---

## 2026-03-01 - US-027
- What was implemented: Ticket listing page at /sac/tickets with filters (status, priority, client, assignee), paginated table, and create ticket dialog. Server actions for listing tickets, creating tickets, listing clients for dropdown, and listing users for assignee dropdown. Redirect from /sac to /sac/tickets. Added Shadcn Textarea component.
- Files changed:
  - erp/src/app/(app)/sac/page.tsx — Redirect page from /sac → /sac/tickets
  - erp/src/app/(app)/sac/tickets/actions.ts — Server actions: listTickets, createTicket, listClientsForSelect, listUsersForAssign
  - erp/src/app/(app)/sac/tickets/page.tsx — Ticket listing page with filters, table, pagination, and create dialog
  - erp/src/components/ui/textarea.tsx — Shadcn Textarea component (added via CLI)
- **Learnings for future iterations:**
  - User→UserCompany Prisma relation is named `userCompanies`, not `companies` — important for filtering users by company access
  - `listUsersForAssign` fetches both ADMIN users (who bypass RBAC) and users with UserCompany assignment to the selected company
  - Ticket create form supports optional proposalId/boletoId fields but they are not exposed in UI yet (will be needed when Proposal/Boleto models exist in US-033+)
  - `/sac` redirect uses server-side `redirect()` from next/navigation — simple pattern for module root pages
---

## 2026-03-01 - US-028
- What was implemented: Ticket detail page at /sac/tickets/[id] with full ticket information, status management, and reassignment
- Files changed:
  - erp/src/app/(app)/sac/tickets/actions.ts — Added server actions: getTicketById, updateTicketStatus, reassignTicket; TicketDetail type; VALID_STATUS_TRANSITIONS map
  - erp/src/app/(app)/sac/tickets/[id]/page.tsx — Ticket detail page with description card, info sidebar (client, company, priority, dates, linked proposal/boleto), status transition buttons, and assignee dropdown
  - erp/src/app/(app)/sac/tickets/page.tsx — Added clickable rows with router navigation to detail page; added useRouter import
- **Learnings for future iterations:**
  - First [id] detail page in the codebase — use `useParams()` from `next/navigation` to get route params, `useRouter().push()` for navigation
  - Status transitions are enforced server-side via `VALID_STATUS_TRANSITIONS` map — OPEN → IN_PROGRESS → WAITING_CLIENT/RESOLVED → CLOSED; prevents invalid transitions
  - Client-side `STATUS_TRANSITIONS` provides user-facing labels for buttons (e.g., "Iniciar Atendimento" for OPEN → IN_PROGRESS)
  - Linked proposal/boleto IDs are displayed with truncated IDs (`slice(-8)`) — will become clickable links once /comercial/propostas/[id] pages exist (US-035+)
  - Reassignment updates ticket in-place via `setTicket` state update — no need to reload the full ticket
---

## 2026-03-01 - US-029
- What was implemented: Unified client history timeline with client detail page
- Files changed:
  - erp/src/app/(app)/comercial/clientes/[id]/actions.ts — Server actions: `getClientById` (fetches client profile) and `getClientTimeline` (aggregates tickets, receivables/boletos, and email messages into unified chronological timeline with optional type filter)
  - erp/src/app/(app)/comercial/clientes/[id]/page.tsx — Client detail page showing profile card (name, razão social, CPF/CNPJ, type, email, phone, address, creation date) and the timeline component
  - erp/src/components/client-timeline.tsx — Reusable `ClientTimeline` component with vertical timeline UI, filter-by-type dropdown (all/ticket/boleto/email), type icons (Headphones/CreditCard/Mail), status badges, clickable "Ver detalhes" links
- **Learnings for future iterations:**
  - Timeline aggregation pattern: query multiple models (Ticket, AccountReceivable, TicketMessage) independently, merge into a flat `TimelineItem[]` array, then sort by date descending — avoids complex SQL unions
  - TicketMessage with `sentViaEmail: true` serves as the "email" type in timeline until a dedicated EmailLog model exists
  - Proposals don't have a model yet (US-033) — the filter includes the type but no data is returned; `getClientTimeline` is structured so adding new entity types (proposals, boletos) only requires adding a new query block
  - The `comercial` directory didn't exist — created as `src/app/(app)/comercial/clientes/[id]/` for the client detail; US-031 will add the listing page and US-032 will enhance the detail page
  - `Number(decimal)` converts Prisma Decimal to JS number for currency formatting in server actions
---

## 2026-03-01 - US-030
- What was implemented: Ticket reply and communication system with chat-style message thread and optional email notification
- Files changed:
  - erp/src/app/(app)/sac/tickets/actions.ts — Added `listTicketMessages`, `createTicketReply` server actions, `TicketMessageRow` and `CreateTicketReplyInput` types; imported `sendEmail` from email service
  - erp/src/app/(app)/sac/tickets/[id]/page.tsx — Added message thread section with chat-style bubbles (sender avatar initial, name, timestamp, email badge), reply form with Textarea and "Send via email" checkbox, scroll-to-bottom on new messages
- **Learnings for future iterations:**
  - The TicketMessage model already existed in schema from US-026 — no migration needed
  - Messages are ordered ASC (oldest first) for natural chat reading flow, with auto-scroll to newest
  - Email sending is fire-and-forget in replies — caught silently so the reply record is always created even if email fails
  - The `sendEmail` service uses ConsoleEmailProvider in dev which just logs to console
  - `useRef` + `scrollIntoView({ behavior: "smooth" })` is the pattern for auto-scrolling to bottom of message thread
---

## 2026-03-01 - US-031
- What was implemented: Full client management CRM page with PF/PJ support
- Files changed:
  - `erp/src/lib/cpf.ts` — New CPF validation utility (stripCpf, formatCpf, isValidCpf)
  - `erp/src/app/(app)/comercial/clientes/actions.ts` — Server actions: listClients (paginated, search by name/CPF/CNPJ/email), createClient, updateClient, getClientForEdit
  - `erp/src/app/(app)/comercial/clientes/page.tsx` — Client listing page with table (name, CPF/CNPJ, email, type), search, pagination, create/edit dialog with PF/PJ type selector, CPF/CNPJ mask and inline validation
  - `erp/src/app/(app)/comercial/page.tsx` — Redirect /comercial → /comercial/clientes
- **Learnings for future iterations:**
  - CPF validation follows same check-digit algorithm pattern as CNPJ but with 11 digits and weights (10-2, 11-2)
  - When switching PF/PJ type in forms, clear the document field to avoid keeping invalid digits from the previous type's mask
  - Client cpfCnpj uniqueness is per company (@@unique([cpfCnpj, companyId])), not global — error messages should reflect this
  - The /comercial route was missing a redirect page; SAC pattern (/sac → /sac/tickets) was the model to follow
  - Clicking client rows navigates to the existing detail page at /comercial/clientes/[id]
---

## 2026-03-01 - US-032
- What was implemented: No code changes needed — the client detail page was already fully built in US-029 (unified client history timeline). Page at `/comercial/clientes/[id]` includes:
  - Client profile card with all fields (CPF/CNPJ, type, email, phone, address, registration date)
  - Interaction history via ClientTimeline component (tickets, boletos, emails)
  - Chronological ordering (most recent first)
  - Filter by type dropdown
  - Clickable items navigate to detail pages
- Files changed: None (only PRD and progress updated)
- **Learnings for future iterations:**
  - US-029 and US-032 had overlapping scope — the client detail page was built as part of the timeline story
  - When proposals are added (US-033+), extend `getClientTimeline` in `src/app/(app)/comercial/clientes/[id]/actions.ts` and add "proposal" to the `TimelineItemType` union
  - The ClientTimeline component is designed for extensibility — adding new timeline item types only requires adding to the server action query and the type/icon/color maps in `src/components/client-timeline.tsx`
---

## 2026-03-01 - US-033
- What was implemented:
  - Added `ProposalStatus` enum (DRAFT/SENT/ACCEPTED/REJECTED/EXPIRED)
  - Added `BoletoStatus` enum (GENERATED/SENT/PAID/OVERDUE/CANCELLED)
  - Added `Proposal` model with clientId, status, paymentConditions, validity, observations, totalValue (Decimal 12,2), companyId
  - Added `ProposalItem` model with proposalId, description, quantity (Decimal 10,2), unitPrice (Decimal 12,2)
  - Added `Boleto` model with proposalId, bankReference, value (Decimal 12,2), dueDate, installmentNumber, status, companyId
  - Updated `Ticket` model to have proper FK relations to Proposal and Boleto (previously just nullable String fields)
  - Updated Company and Client models with new relation arrays
  - Migration generated and applied successfully
- Files changed:
  - `erp/prisma/schema.prisma` — added enums, models, updated relations
  - `erp/prisma/migrations/20260301013159_add_proposal_boleto_models/migration.sql` — new migration
- **Learnings for future iterations:**
  - Ticket.proposalId and Ticket.boletoId now have proper FK relations via named relations ("TicketProposal", "TicketBoleto") — use `include: { proposal: true, boleto: true }` to load them
  - Proposal.totalValue uses `@default(0)` and should be computed as sum of items (quantity * unitPrice) in server actions
  - ProposalItem.quantity uses `Decimal(10,2)` to support fractional quantities (e.g., 1.5 hours of service)
  - When implementing US-034+ proposals, extend `getClientTimeline` to include proposals (as noted in US-032 learnings)
---

## 2026-03-01 - US-034
- What was implemented: Proposal CRUD server actions with full validation, status transitions, and auto-calculated totals
- Files changed:
  - `erp/src/app/(app)/comercial/propostas/actions.ts` — new file with all proposal server actions
- Server actions created:
  - `createProposal` — creates proposal with client + items, auto-calculates totalValue
  - `updateProposal` — updates proposal (DRAFT only), replaces items in transaction
  - `listProposals` — paginated listing with filters (status, client, date range, value range)
  - `getProposalById` — fetches proposal with all items and client name
  - `updateProposalStatus` — enforces valid status transitions (DRAFT→SENT→ACCEPTED/REJECTED/EXPIRED)
  - `listClientsForProposal` — client dropdown helper
- **Learnings for future iterations:**
  - Proposal totalValue is auto-computed from items; stored as Decimal(12,2) in DB
  - Status transitions map: DRAFT→SENT, SENT→ACCEPTED/REJECTED/EXPIRED; terminal states have no further transitions
  - Update replaces all items in a transaction (delete old + create new) — simpler than diffing
  - Only DRAFT proposals can be edited; sent/accepted/rejected proposals are immutable
  - Proposal items are ordered by `id` (creation order) in getProposalById
  - `listClientsForProposal` provides client dropdown data for proposal forms
---

## 2026-03-01 - US-035
- Implemented proposal form page at `/comercial/propostas/nova`
- Client selector with search filtering and link to create new client
- Dynamic item list: add/remove items with description, quantity, unit price
- Auto-calculated subtotals per item and total displayed in real-time using `formatBRL`
- Fields: payment conditions, validity date, observations
- Save as draft button (creates proposal with DRAFT status)
- Edit mode via `?edit=<id>` query param — loads existing proposal data
- Files changed:
  - `erp/src/app/(app)/comercial/propostas/nova/page.tsx` (new)
- **Learnings for future iterations:**
  - Proposal form uses `useSearchParams().get("edit")` for edit mode — pass `?edit=<proposalId>` to reuse form for editing
  - Items use a `key` field (random string) for React list keys since they don't have DB IDs before saving
  - Client selector uses `Select` + embedded `Input` for search inside the dropdown content
  - Reuses existing `createProposal` and `updateProposal` server actions from `../actions`
  - `calculateTotal` is computed client-side for real-time display; server action recalculates independently
---

## 2026-03-01 - US-036
- What was implemented: Proposal listing page at `/comercial/propostas` with full table display (client, total value, status, date, actions), status badges with colors (gray=draft, blue=sent, green=accepted, red=rejected, orange=expired), filters (status, client, date range, value range), row click navigation to edit/view proposals, and pagination
- Files changed:
  - `erp/src/app/(app)/comercial/propostas/page.tsx` — new listing page component
- **Learnings for future iterations:**
  - Proposal listing reuses the same pattern as financial listing pages (receber/pagar) — filter panel with collapsible toggle, paginated table, status badges
  - Row click navigates to `/comercial/propostas/nova?edit=<id>` for both draft (edit) and non-draft (view) proposals — the edit page handles read-only display for non-DRAFT statuses
  - Value range filters use `filterValueMin`/`filterValueMax` parsed to float before passing to server actions
  - All types needed (`PaginatedResult`, `ProposalRow`, `ClientOption`) are already exported from the existing `actions.ts`
---

## 2026-03-01 - US-037
- What was implemented: Abstract boleto generation service with provider pattern, PinBank mock adapter, and server actions for generating boletos from accepted proposals with automatic AccountReceivable creation
- Files changed:
  - `erp/src/lib/boleto.ts` — NEW: Abstract BoletoProvider interface, GenerateBoletoInput/Result types, MockBoletoProvider (PinBank mock), setBoletoProvider() and generateBoleto() public functions
  - `erp/src/app/(app)/comercial/propostas/actions.ts` — UPDATED: Added generateBoletosForProposal(), listBoletosForProposal(), updateBoletoStatus() server actions with BoletoRow type
- **Learnings for future iterations:**
  - Boleto generation follows same provider pattern as email service in `src/lib/email.ts` — abstract interface with mock implementation
  - `generateBoletosForProposal` validates proposal is ACCEPTED and has no existing boletos before generating
  - Installment values are split evenly with rounding adjustment on last installment
  - Each boleto automatically creates a corresponding AccountReceivable entry in the same transaction
  - Boleto status transitions: GENERATED → SENT/CANCELLED, SENT → PAID/OVERDUE/CANCELLED, OVERDUE → PAID/CANCELLED; PAID and CANCELLED are terminal
  - Boleto server actions are in the proposals actions file (`comercial/propostas/actions.ts`) since boletos are tightly coupled to proposals
---

## 2026-03-01 - US-038
- What was implemented: Boleto generation UI with proposal detail page
- Files changed:
  - erp/src/app/(app)/comercial/propostas/[id]/page.tsx — NEW: proposal detail page showing proposal info, items table, and boletos section with generation dialog
  - erp/src/app/(app)/comercial/propostas/page.tsx — Updated row click to navigate to detail page for non-DRAFT proposals
- **Learnings for future iterations:**
  - Proposal detail page at `/comercial/propostas/[id]` shows proposal info + items + boletos
  - Non-DRAFT proposals navigate to detail page; DRAFT proposals navigate to edit form
  - Boleto status colors: gray=GENERATED, blue=SENT, green=PAID, red=OVERDUE, orange=CANCELLED
  - `generateBoletosForProposal` from actions.ts handles all boleto creation logic; UI just passes installments count and first due date
  - Boletos can only be generated for ACCEPTED proposals with no existing boletos
  - Dialog pattern: use state for dialog open, form fields, and loading state; reset on close
---

## 2026-03-01 - US-039
- What was implemented: Email template system with company branding and server actions for sending proposal and boleto emails
- Files changed:
  - erp/src/lib/email-templates.ts — New file: HTML email template builder with company branding (logo, colors, contact info), proposal template, and boleto template
  - erp/src/lib/email-actions.ts — New file: Server actions `sendProposalEmail(proposalId, companyId)` and `sendBoletoEmail(boletoId, companyId)` with text-based PDF attachments, status updates, and audit logging
- **Learnings for future iterations:**
  - Email templates use `buildEmailHtml()` base function that wraps content in branded header/footer; specific templates (`buildProposalEmailHtml`, `buildBoletoEmailHtml`) build body content
  - Company branding data: `nomeFantasia`, `logoUrl`, `email`, `telefone` — fetched from Company model via Prisma include
  - `sendProposalEmail` auto-transitions DRAFT→SENT status; `sendBoletoEmail` auto-transitions GENERATED→SENT status
  - Both actions log two audit events: one for status change (if applicable) and one for email send (entity="Email")
  - PDF attachments are text-based for server-side compatibility; client-side PDF generation uses jspdf (see DRE export pattern)
  - Email actions are in `src/lib/email-actions.ts` (not in page-specific actions.ts) since they're shared across modules
  - Client must have email field populated or the action throws "Cliente não possui e-mail cadastrado"
---

## 2026-03-01 - US-040
- What was implemented: Added email sending UI for proposals and boletos on the proposal detail page
- Files changed:
  - erp/src/app/(app)/comercial/propostas/[id]/page.tsx — Added "Enviar por E-mail" button for proposals (DRAFT/SENT status), per-boleto email send buttons (GENERATED/SENT status), confirmation dialogs with recipient email/subject preview for both
  - erp/src/app/(app)/comercial/propostas/actions.ts — Extended ProposalDetail type with clientEmail field, updated getProposalById to include client email in query
- **Learnings for future iterations:**
  - ProposalDetail now includes `clientEmail` — used to show recipient in confirmation dialog and conditionally display email buttons
  - Email send buttons check `proposal.clientEmail` — if null, buttons are hidden (email actions throw if no email)
  - Confirmation dialogs show: recipient email, subject preview, attachment filename, and status transition notice
  - Email sending reloads page data via `loadData()` to reflect status changes (DRAFT→SENT, GENERATED→SENT)
  - `selectedCompany` from `useCompany()` provides `nomeFantasia` for building subject preview on the client side
  - Boleto email dialog uses `selectedBoletoForEmail` state to track which boleto is being sent; cleared on dialog close
---
