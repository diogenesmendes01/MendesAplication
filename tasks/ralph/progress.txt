# Ralph Progress — MendesERP (whatsapp-baileys-ai-agent)

## Codebase Patterns
- Project lives in `/workspaces/app/erp/` — all npm/npx commands must run from there
- WhatsApp Service lives in `/workspaces/app/whatsapp-service/` — separate Express microservice
- Next.js 14.2.35 with App Router, TypeScript strict mode
- TailwindCSS 3.x with Shadcn/UI (New York style, default config)
- Prisma 6.x with `prisma-client-js` generator (classic, not new `prisma-client`)
- Prisma singleton in `src/lib/prisma.ts` — import from `@/lib/prisma`
- Shadcn/UI components in `src/components/ui/` — use Sonner for toasts (not the old Toaster)
- Quality checks: `npx tsc --noEmit` for typecheck, `npm run lint` for ESLint
- PostgreSQL needs `sudo service postgresql start` before db operations in devcontainer
- Prisma compound unique with nullable fields: use `findFirst` + `create` instead of `upsert`
- Prisma JSON fields need `as Prisma.InputJsonValue` cast for Record<string, unknown>
- `.env` is gitignored; `.env.example` has the template
- Working directory for git commands: `/workspaces/app/` (root repo)
- Prisma seed config goes in `package.json` under `"prisma": { "seed": "npx tsx prisma/seed.ts" }`
- Use `upsert` in seed scripts for idempotent re-runs
- Shared enums (Status, UserRole) are defined at schema level and reused across models
- Auth utilities in `src/lib/auth.ts` — import `hashPassword`, `comparePassword`, `generateAccessToken`, `generateRefreshToken`, `verifyAccessToken`, `getCurrentUser`
- Session helpers in `src/lib/session.ts`: `getSession()`, `requireSession()`, `requireAdmin()`
- RBAC helpers in `src/lib/rbac.ts`: `canAccessCompany()`, `requireCompanyAccess()`
- Server actions use `"use server"` directive and live alongside their pages (e.g. `sac/tickets/actions.ts`)
- Company context: `useCompany()` hook from `@/contexts/company-context` gives `selectedCompanyId`, `selectedCompany`, `companies`
- BullMQ queues defined in `src/lib/queue.ts`, workers in `src/lib/workers/`
- Redis at localhost:6379 for BullMQ
- WhatsApp Service and ERP share same PostgreSQL database
- WhatsApp Service communicates with ERP via HTTP webhooks (localhost)

- Baileys v7 requires Node.js 20+ — devcontainer has Node 18 by default, need `sudo npx -y n 20` to upgrade
- WhatsApp Service uses ESM (`"type": "module"` in package.json) and `moduleResolution: "bundler"` in tsconfig
- When schema.prisma changes in ERP, must copy to whatsapp-service/prisma/ and run `npx prisma generate` there too
- Prisma JSON fields need `as Prisma.InputJsonValue` cast (import `Prisma` from `@prisma/client`)
- Baileys keys.get must use `<T extends keyof SignalDataTypeMap>` generic signature for type compatibility
- BaileysProvider is a singleton exported as `baileysProvider` — use it from routes/controllers
- BaileysProvider public API: initiateQrCode, initiatePairingCode, getQrCode, getPairingCode, disconnect, getConnectionStatus, sendMessage, sendMediaMessage
- Webhook payload must match EvolutionWebhookPayload format (event, instance, data.key, data.message, data.media)
- ERP webhook route validates `apikey` header — WhatsApp Service sends WHATSAPP_WEBHOOK_SECRET in that header
- Media files saved to uploads/{companyId}/ and served via Express static at /uploads/
- getContentType from Baileys returns message type key; messages.upsert type="notify" for real-time messages
- WhatsApp Service Express routes: /health (no auth), /instance/* and /message/* (require apikey header)
- Express v5 async handlers: don't return res.json(), use void return type for proper typing
- DisconnectReason codes: loggedOut=401, timedOut=408, connectionClosed=428, restartRequired=515
- Baileys makeWASocket requires `version` from fetchLatestBaileysVersion() and `makeCacheableSignalKeyStore` wrapper
- Use `socket.end(undefined)` for graceful disconnect, NOT `socket.logout()` (logout removes the account)

## Progress Log

## 2026-03-03 - US-001
- Scaffolded whatsapp-service/ directory with package.json, tsconfig.json, .env.example, .gitignore, src/index.ts
- Dependencies: @whiskeysockets/baileys v7, express v5, @prisma/client, @hapi/boom, qrcode, cors, dotenv
- DevDeps: @types/express, @types/cors, @types/qrcode, prisma, tsx, typescript
- npm install runs successfully, typecheck passes
- Files created: whatsapp-service/package.json, tsconfig.json, .env.example, .gitignore, src/index.ts
- **Learnings for future iterations:**
  - Baileys v7 requires Node 20+ — install via `sudo npx -y n 20 && hash -r` if on Node 18
  - WhatsApp Service is ESM (type: module) with bundler moduleResolution
  - Reference code from JaRespondi is at /workspaces/app/temp/jarespondiv2/
---

## 2026-03-03 - US-002
- Added BaileysAuthState model to schema.prisma: id (cuid), companyId, keyType, keyId, keyData (Json), createdAt, updatedAt
- Added LidMapping model to schema.prisma: id (cuid), companyId, lid, phoneNumber, createdAt, updatedAt
- BaileysAuthState has @@unique([companyId, keyType, keyId]) and @@index([companyId])
- LidMapping has @@unique([companyId, lid]) and @@index([companyId])
- Both models have onDelete: Cascade relation to Company
- Added baileysAuthStates and lidMappings relations to Company model
- @@map to baileys_auth_state and lid_mappings
- prisma db push executed without errors, typecheck passes
- Files changed: erp/prisma/schema.prisma
- **Learnings for future iterations:**
  - Adding new models with Company relations follows the same pattern: add model at end of schema, add relation field in Company model
  - prisma db push auto-runs generate, so no separate generate step needed
---

## 2026-03-03 - US-003
- Copied schema.prisma from erp/prisma/ to whatsapp-service/prisma/
- Ran npx prisma generate successfully in whatsapp-service directory
- Created whatsapp-service/src/lib/prisma.ts with singleton PrismaClient pattern (identical to ERP pattern)
- Typecheck passes in both whatsapp-service and erp
- Files created: whatsapp-service/prisma/schema.prisma, whatsapp-service/src/lib/prisma.ts
- **Learnings for future iterations:**
  - When schema.prisma changes in ERP, must also copy to whatsapp-service/prisma/ and re-run prisma generate there
  - Both services share the same PostgreSQL database via DATABASE_URL
  - whatsapp-service prisma generate creates its own node_modules/@prisma/client
---

## 2026-03-03 - US-004
- Implemented whatsapp-service/src/providers/useDatabaseAuthState.ts
- Adapted from JaRespondi reference: replaced all Supabase calls with Prisma equivalents
- Key changes from reference: whatsapp_number_id → companyId, supabase.from() → prisma.baileysAuthState/lidMapping
- Features: in-memory cache (Map), BufferJSON serialization/deserialization, all key types supported
- Key types: creds, pre-key, session, sender-key, sender-key-memory, app-state-sync-key, app-state-sync-version, tctoken, device-list, lid-mapping
- LID mappings stored in dedicated LidMapping table via Prisma upsert
- saveCreds() persists credentials via upsert with BufferJSON.replacer
- readKey, writeKey, removeKey with cache invalidation
- Typecheck passes in both whatsapp-service and erp
- Files created: whatsapp-service/src/providers/useDatabaseAuthState.ts
- **Learnings for future iterations:**
  - Prisma JSON fields need `as Prisma.InputJsonValue` cast (not `Record<string, unknown>`)
  - Baileys SignalDataTypeMap generic typing: use `<T extends keyof SignalDataTypeMap>` for keys.get signature
  - The `set` callback from Baileys uses a complex nested type — use `any` for the data param to avoid type gymnastics
  - keyType is derived from keyId prefix (e.g., "session-" → "session") for the compound unique constraint
  - Use `deleteMany` instead of `delete` when not using the unique constraint directly
  - Use `findFirst` instead of `findUnique` when querying by partial fields of a compound unique
---

## 2026-03-03 - US-005
- Implemented whatsapp-service/src/providers/baileys.provider.ts with BaileysProvider class
- Connection management: initiateQrCode, initiatePairingCode, getQrCode, getPairingCode, disconnect, getConnectionStatus
- Session tracking via Map<string, BaileysSession> with isConnected/isConnecting/lastError
- Cooldown: 15-second throttle between init attempts per company (bypassable)
- Exponential backoff: 5s -> 60s max for reconnection when previously connected
- DisconnectReason handling:
  - 401/loggedOut/device_removed: NEVER reconnect, clear auth state
  - 515/restartRequired: immediate reconnect via setImmediate with same credentials
  - 408/timedOut (never connected): no reconnect, QR expired message
  - Was connected + other: exponential backoff reconnect
  - Never connected + other: no reconnect
- QR code generation via `qrcode` lib (toDataURL -> base64)
- Pairing code: requestPairingCode -> formatted as XXXX-XXXX
- Pairing uses Browsers.macOS('Chrome'), QR uses Browsers.ubuntu('Chrome')
- Socket config: connectTimeoutMs 30s, defaultQueryTimeoutMs 15s, keepAliveIntervalMs 25s
- getSession() method exposed for US-006 message sending
- Singleton export: baileysProvider
- Files created: whatsapp-service/src/providers/baileys.provider.ts
- **Learnings for future iterations:**
  - DisconnectReason enum: loggedOut=401, timedOut=408, connectionClosed=428, connectionReplaced=440, restartRequired=515
  - Baileys makeWASocket requires `version` from fetchLatestBaileysVersion()
  - makeCacheableSignalKeyStore wraps state.keys for better performance
  - Pairing code method requires Browsers.macOS (not ubuntu) per Baileys docs
  - `socket.end(undefined)` is the graceful disconnect method
  - QR mode: connection.update with `qr` field provides the raw QR string to encode
  - Pairing mode: must wait ~2s after socket creation before calling requestPairingCode
  - The `getMessage` callback is required by Baileys for message retries/edits
---

## 2026-03-03 - US-006
- Added sendMessage(companyId, to, content) method: validates session, normalizes JID, calls socket.sendMessage with text content, returns messageId
- Added sendMediaMessage(companyId, to, mediaUrl, caption, mediaType) method: supports image/video/audio/document types with appropriate AnyMessageContent objects
- Added messages.upsert event handler in setupMessageHandler: only processes type="notify" (real-time), skips fromMe, status@broadcast, group messages, and protocol messages
- Added handleMessage: detects message type (conversation, extendedTextMessage, imageMessage, videoMessage, audioMessage, documentMessage), extracts text content and media info
- Added downloadAndSaveMedia: uses Baileys downloadMediaMessage to decrypt media, saves to uploads/{companyId}/ with timestamped filename, returns public URL for Express static serving
- Added webhook dispatch (dispatchWebhook): POSTs to WHATSAPP_WEBHOOK_URL with `apikey` header containing WHATSAPP_WEBHOOK_SECRET
- Webhook payload matches EvolutionWebhookPayload format: { event: "messages.upsert", instance: companyId, data: { key, pushName, message, messageType, messageTimestamp, media? } }
- Media is served via `data.media.url` field (Evolution API v2 format), which the ERP inbound worker already supports
- Added LID resolution (resolveLid): detects LID JIDs (containing ":" or "@lid"), queries LidMapping table, returns resolved phone@s.whatsapp.net
- Added saveLidMapping: persists LID-to-phone mappings from messaging-history.set events
- Added normalizeJid helper: strips non-digits, appends @s.whatsapp.net
- Added buildMessageField: constructs simplified message object for webhook matching EvolutionWebhookPayload.data.message structure
- New imports: downloadMediaMessage, getContentType, WAMessage, AnyMessageContent from Baileys; fs/promises, path from Node.js
- Environment variables: WHATSAPP_WEBHOOK_URL, WHATSAPP_WEBHOOK_SECRET, WHATSAPP_SERVICE_BASE_URL
- Files changed: whatsapp-service/src/providers/baileys.provider.ts (+508 lines)
- Typecheck passes in both whatsapp-service and erp
- **Learnings for future iterations:**
  - Webhook payload MUST match EvolutionWebhookPayload interface in erp/src/lib/workers/whatsapp-inbound.ts (event, instance, data.key, data.message, data.media)
  - ERP webhook route validates `apikey` header (not `x-api-secret` like JaRespondi reference)
  - ERP inbound worker checks `data.media?.url` first (Evolution v2 format) before falling back to message-level media URLs
  - Use `instance: companyId` in webhook payload — US-008 will update channel config to match on companyId
  - downloadMediaMessage requires { logger, reuploadRequest } config — use `undefined as never` for logger when no logger available
  - getContentType from Baileys returns the message type key (e.g., "conversation", "imageMessage", "protocolMessage")
  - Baileys messages.upsert has `type: "notify"` for real-time and `type: "append"` for history sync — only process "notify"
---

## 2026-03-03 - US-007
- Implemented full Express server in whatsapp-service/src/index.ts with all required routes
- Health check: GET /health (no auth, returns 200)
- API key auth middleware: validates `apikey` header against WHATSAPP_SERVICE_API_KEY env
- Instance routes: POST /instance/connect, POST /instance/connect-pairing, GET /instance/:companyId/qr, GET /instance/:companyId/pairing-code, GET /instance/:companyId/status, POST /instance/:companyId/disconnect
- Message routes: POST /message/send-text, POST /message/send-media
- Static file serving: uploads/ directory served at /uploads/ path
- Graceful shutdown: SIGTERM/SIGINT handlers with prisma.$disconnect()
- Server listens on WHATSAPP_SERVICE_PORT (default 3001)
- Typecheck passes in both whatsapp-service and erp
- Files changed: whatsapp-service/src/index.ts (complete rewrite from placeholder)
- **Learnings for future iterations:**
  - Express v5 route handlers should use explicit void return (not return res.json()) for proper typing
  - API key auth uses `apikey` header (lowercase) to match what the ERP sends in webhook calls
  - Health endpoint must be registered BEFORE auth middleware so it doesn't require authentication
  - Express static middleware for uploads must also be before auth middleware for public media access
---
