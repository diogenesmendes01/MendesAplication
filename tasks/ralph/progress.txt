# Ralph Progress — MendesERP (sac-omnichannel)

## Codebase Patterns
- Project lives in `/workspaces/app/erp/` — all npm/npx commands must run from there
- Next.js 14.2.35 with App Router, TypeScript strict mode
- TailwindCSS 3.x with Shadcn/UI (New York style, default config)
- Prisma 6.x with `prisma-client-js` generator (classic, not new `prisma-client`)
- Prisma singleton in `src/lib/prisma.ts` — import from `@/lib/prisma`
- Shadcn/UI components in `src/components/ui/` — use Sonner for toasts (not the old Toaster)
- Quality checks: `npx tsc --noEmit` for typecheck, `npm run lint` for ESLint
- PostgreSQL needs `sudo service postgresql start` before db operations in devcontainer
- Prisma compound unique with nullable fields: use `findFirst` + `create` instead of `upsert`
- Prisma JSON fields need `as Prisma.InputJsonValue` cast for Record<string, unknown>
- `.env` is gitignored; `.env.example` has the template
- Working directory for git commands: `/workspaces/app/` (root repo)
- Prisma seed config goes in `package.json` under `"prisma": { "seed": "npx tsx prisma/seed.ts" }`
- Use `upsert` in seed scripts for idempotent re-runs
- Shared enums (Status, UserRole) are defined at schema level and reused across models
- Auth utilities in `src/lib/auth.ts` — import `hashPassword`, `comparePassword`, `generateAccessToken`, `generateRefreshToken`, `verifyAccessToken`, `getCurrentUser`
- Session helpers in `src/lib/session.ts`: `getSession()`, `requireSession()`, `requireAdmin()`
- RBAC helpers in `src/lib/rbac.ts`: `canAccessCompany()`, `requireCompanyAccess()`
- Server actions use `"use server"` directive and live alongside their pages (e.g. `sac/tickets/actions.ts`)
- Company context: `useCompany()` hook from `@/contexts/company-context` gives `selectedCompanyId`, `selectedCompany`, `companies`
- Company-scoped server actions accept `companyId` param and validate access via `requireCompanyAccess(companyId)`
- Navigation items defined in `navItems` array in sidebar.tsx — add new nav links there
- Listing pages: client component with useCallback for loadData, useEffect to trigger, search resets to page 1
- Form dialogs: reuse same Dialog for create/edit, distinguish via `editingId` state
- Audit logging on all mutations via AuditLog model
- Abstract interfaces for email provider in `src/lib/email.ts`
- Design doc: `docs/plans/2026-03-02-sac-omnichannel-design.md`
- PRD: `tasks/prd-sac-omnichannel.md`
- Redis devcontainer feature: `ghcr.io/itsmechlark/features/redis-server:1` — after devcontainer rebuild, redis-server is auto-started
- REDIS_URL env var: `redis://localhost:6379` — used by BullMQ and ioredis

## 2026-03-02 - US-048
- Installed Redis via devcontainer feature (`ghcr.io/itsmechlark/features/redis-server:1`)
- Added REDIS_URL=redis://localhost:6379 to .env.example
- Verified redis-cli ping returns PONG
- Typecheck passes
- Files changed: `.devcontainer/devcontainer.json`, `erp/.env.example`
- **Learnings for future iterations:**
  - The devcontainer feature `ghcr.io/itsmechlark/features/redis-server:1` installs and auto-starts Redis on port 6379
  - In a running container, the feature won't take effect until rebuild — install redis-server manually with apt for immediate testing
  - `.env` is gitignored; always update `.env.example` for template and `.env` locally for runtime
---

## 2026-03-02 - US-049
- Configured BullMQ infrastructure with 5 named queues: email-inbound, email-outbound, whatsapp-inbound, whatsapp-outbound, sla-check
- Created `erp/src/lib/queue.ts` with Redis connection config (parses REDIS_URL env var) and queue exports
- Created `erp/src/lib/workers/base.ts` with `createWorker()` factory function that logs job processing
- Created `erp/src/lib/workers/index.ts` with worker instances for all 5 queues and graceful shutdown
- Added `"workers"` npm script: `npx tsx src/lib/workers/index.ts`
- Added bullmq@5.70.1 dependency (ioredis@5.9.3 comes as transitive dep)
- Files changed: `erp/package.json`, `erp/package-lock.json`, `erp/src/lib/queue.ts`, `erp/src/lib/workers/base.ts`, `erp/src/lib/workers/index.ts`
- **Learnings for future iterations:**
  - BullMQ connection config uses plain object `{ host, port, password, maxRetriesPerRequest: null }` — maxRetriesPerRequest must be null for workers
  - Workers run via `npx tsx` from erp/ directory — the `@/` path alias works because tsx respects tsconfig paths
  - `createWorker()` factory in base.ts handles logging, error handling, and events — use it for all new workers
  - Queue names are constants in `QUEUE_NAMES` object — import from `@/lib/queue`
  - Graceful shutdown in index.ts listens for SIGINT/SIGTERM and closes all workers
---

## 2026-03-02 - US-050
- Created `docker-compose.yml` at project root with evolution-api service (atydoc/evolution-api:latest) on port 8080
- Added EVOLUTION_API_URL and EVOLUTION_API_KEY to `erp/.env.example` with explanatory comments
- Docker compose uses named volumes for persistence (evolution_instances, evolution_store)
- Files changed: `docker-compose.yml`, `erp/.env.example`
- **Learnings for future iterations:**
  - Evolution API uses `AUTHENTICATION_API_KEY` env var internally — map from our `EVOLUTION_API_KEY`
  - Start Evolution API with `docker compose up -d evolution-api` from project root
  - Evolution API default port is 8080
---

## 2026-03-02 - US-051
- Added 10 new enums to schema.prisma: ChannelType, MessageDirection, MessageOrigin, SlaType, RefundStatus, RefundPaymentMethod, RefundAttachmentType, RefundInvoiceAction, PayableOrigin, InvoiceType
- Added CANCELLED to ProposalStatus enum
- InvoiceStatus already had CANCELLED
- Fixed VALID_STATUS_TRANSITIONS in propostas/actions.ts to include CANCELLED state
- Applied schema changes via `npx prisma db push`
- Files changed: `erp/prisma/schema.prisma`, `erp/src/app/(app)/comercial/propostas/actions.ts`
- **Learnings for future iterations:**
  - Adding values to enums used in Record<EnumType, ...> maps will cause TS errors — update all exhaustive maps
  - ProposalStatus transitions map is in `comercial/propostas/actions.ts` line ~91
  - PostgreSQL needs to be started with `sudo service postgresql start` before db push
---

## 2026-03-02 - US-052
- Created AdditionalContact model: id (cuid), clientId, name, role?, email?, whatsapp?, createdAt
- Added relation Client.additionalContacts (1:N, onDelete Cascade)
- Added sample clients (ABC Tecnologia, XYZ Serviços) and 3 additional contacts to seed
- Used createMany for bulk contact creation with idempotent check
- Files changed: `erp/prisma/schema.prisma`, `erp/prisma/seed.ts`
- **Learnings for future iterations:**
  - Seed used `findMany` + check length for idempotent createMany (since createMany doesn't support upsert)
  - Client unique constraint is `cpfCnpj_companyId` — use this for upsert in seeds
---

## 2026-03-02 - US-053
- Created Channel model with all fields: id, companyId, type (ChannelType), name, config (Json), isActive, lastSyncUid, lastSyncUidSent, lastSyncAt, createdAt, updatedAt
- Added Company.channels relation (1:N, onDelete Cascade)
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-054
- Created SlaConfig model with unique constraint [companyId, type, priority, stage]
- Added Company.slaConfigs relation (1:N, onDelete Cascade)
- Seeded 9 SLA configs per company: 6 ticket SLAs (3 priorities x 2 stages) + 3 refund SLAs
- Files changed: `erp/prisma/schema.prisma`, `erp/prisma/seed.ts`
- **Learnings for future iterations:**
  - Prisma compound unique with nullable fields doesn't work well with `upsert` — use `findFirst` + `create` pattern instead
  - SLA deadlines in minutes: Alta first_reply=30, resolution=240; Media 120/1440; Baixa 480/2880
  - Refund SLAs use priority=null since they don't vary by priority
---

## 2026-03-02 - US-055
- Created Attachment model with ticketMessageId/ticketId (both optional), fileName, fileSize, mimeType, storagePath
- Added named relations: TicketMessage.attachments ("TicketMessageAttachments"), Ticket.attachments ("TicketAttachments")
- Created `erp/uploads/` directory with `.gitkeep`; added gitignore rules for uploads content
- Files changed: `erp/prisma/schema.prisma`, `.gitignore`, `erp/uploads/.gitkeep`
---

## 2026-03-02 - US-056
- Created Refund model with all fields: id, ticketId, companyId, requestedById, approvedById, executedById, amount, paymentMethod, bank details, pixKey, status, rejectionReason, timestamps, SLA fields, invoice action
- Created RefundAttachment model with type (RefundAttachmentType), file info, uploadedById
- Added named relations on User: refundsRequested/Approved/Executed
- Added refunds relation to Ticket and Company
- Note: accountPayable and creditNote back-relations to be added in US-059
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-057
- Added channelId, contactId, slaFirstReply, slaResolution, slaBreached, tags fields to Ticket
- Added channel (Channel), contact (AdditionalContact) relations to Ticket
- Added tickets[] back-relations to Channel and AdditionalContact
- Seed continues working without errors
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-058
- Added channel (ChannelType?), direction (MessageDirection), origin (MessageOrigin), externalId, contactId, isInternal fields to TicketMessage
- Added contact relation (AdditionalContact) with named relation "TicketMessageContact"
- Added @@unique([externalId, channel]) for deduplication
- Used `--accept-data-loss` flag for db push when adding unique constraint on existing table
- Files changed: `erp/prisma/schema.prisma`
- **Learnings for future iterations:**
  - Adding unique constraints to existing tables may require `--accept-data-loss` flag with `prisma db push`
  - Named relations needed when model has multiple references to same target (e.g., AdditionalContact referenced by both Ticket and TicketMessage)
---

## 2026-03-02 - US-059
- Added origin (PayableOrigin), refundId (unique) to AccountPayable with Refund relation
- Added type (InvoiceType), cancelledAt, cancellationReason, refundId (unique), originalInvoiceId to Invoice
- Added self-relation "CreditNote" on Invoice for credit notes
- Added accountPayable and creditNote back-relations to Refund (completing US-056 deferred work)
- Files changed: `erp/prisma/schema.prisma`
---

## 2026-03-02 - US-060
- Created contacts-actions.ts with 4 server actions: listAdditionalContacts, createAdditionalContact, updateAdditionalContact, deleteAdditionalContact
- All actions validate company access via requireCompanyAccess
- Create/update validate at least email or whatsapp is provided
- All mutations include audit logging via logAuditEvent
- Access validation checks contact's client.companyId matches requested companyId
- Files changed: `erp/src/app/(app)/comercial/clientes/[id]/contacts-actions.ts`
---

## 2026-03-02 - US-061
- Created AdditionalContacts component with list, create/edit dialog, and delete confirmation
- Shows name, role, email, whatsapp for each contact with edit/delete buttons
- Integrated into client detail page between profile card and timeline
- Toast notifications for success/error on all operations
- Browser verification not available (no dev-browser MCP tool)
- Files changed: `erp/src/app/(app)/comercial/clientes/[id]/additional-contacts.tsx`, `.../[id]/page.tsx`
---

## 2026-03-02 - US-062
- Created `erp/src/lib/file-upload.ts` with uploadFile, getFileUrl, deleteFile, getFilePath, getMimeType
- Created POST /api/upload route: receives FormData, validates auth/file type/size, saves to uploads/{companyId}/{year-month}/
- Created GET /api/files/[...path] route: serves files with auth check and path traversal prevention
- 10MB limit, supports PDF/PNG/JPG/JPEG/GIF/DOC/DOCX/XLS/XLSX/CSV/TXT
- Files sanitized with timestamp suffix for uniqueness
- Files changed: `erp/src/lib/file-upload.ts`, `erp/src/app/api/upload/route.ts`, `erp/src/app/api/files/[...path]/route.ts`
- **Learnings for future iterations:**
  - Next.js App Router catch-all routes use `[...path]` directory pattern
  - In Next.js 14.2, route handler params are `Promise<{path: string[]}>` — must await
  - File serving uses `new NextResponse(buffer)` directly for binary responses
  - Path traversal prevention: check for `..` in storage paths
---

## 2026-03-02 - US-063
- Created channel server actions: listChannels, createChannel, updateChannel, toggleChannel, testChannelConnection
- Implemented AES-128-CBC encryption for sensitive config fields (password, apiKey) using ENCRYPTION_KEY env var
- Admin validation via requireAdmin() for all mutation/test actions
- testChannelConnection validates config completeness for EMAIL and tests Evolution API for WHATSAPP
- Added ENCRYPTION_KEY to .env.example
- Files changed: `erp/src/app/(app)/configuracoes/canais/actions.ts`, `erp/.env.example`
- **Learnings for future iterations:**
  - Prisma JSON fields need `as Prisma.InputJsonValue` cast for Record<string, unknown>
  - Use `requireAdmin()` from session.ts for admin-only actions (not requireSession)
  - Channel config encryption: iv:ciphertext format with AES-128-CBC
  - Decrypt wraps in try-catch to handle unencrypted legacy values
---

## 2026-03-02 - US-064
- Created /configuracoes/canais page with channel management UI
- Channel cards showing: name, type icon (Mail/MessageSquare), address, active status badge, last sync timestamp
- Create/Edit dialog with type-specific fields: IMAP/SMTP for email, instanceName/apiUrl/apiKey for WhatsApp
- Test connection button with inline result display
- Activate/deactivate toggle per channel
- Company-scoped via useCompany context
- Files changed: `erp/src/app/(app)/configuracoes/canais/page.tsx`
---

## 2026-03-02 - US-065
- Created SLA config server actions: getSlaConfigs, saveSlaConfigs, getBusinessHours, saveBusinessHours
- getSlaConfigs returns defaults (with null IDs) if no company config exists
- saveSlaConfigs uses transaction: delete all + createMany for atomic batch update
- Business hours stored as special SlaConfig entry with stage="business_hours", encoded in existing fields
- All mutations require admin access and include audit logging
- Files changed: `erp/src/app/(app)/configuracoes/sla/actions.ts`
- **Learnings for future iterations:**
  - Business hours encoded: deadlineMinutes = startHour*100+endHour, alertBeforeMinutes = workDays bitmask
  - Default SLA values defined as constants for reuse across actions and seed
---

## 2026-03-02 - US-066
- Created /configuracoes/sla page with editable SLA tables and business hours
- Ticket SLA table: rows per priority (Alta/Média/Baixa) x stage (1ª Resposta/Resolução)
- Refund SLA table: rows per stage (Aprovação/Execução/Total)
- Business hours section with enable toggle, start/end hour, day-of-week checkboxes
- Single Save button persists SLA configs and business hours atomically
- Shows default values when no config exists for company
- Files changed: `erp/src/app/(app)/configuracoes/sla/page.tsx`
---

## 2026-03-02 - US-067
- Created `erp/src/lib/sla.ts` with 4 exported functions + BusinessHours interface + SlaStatusValue type
- `calculateSlaDeadline(startTime, minutes, businessHours?)` — adds minutes to startTime, skipping non-business hours when BusinessHours is provided and enabled
- `isSlaBreached(deadline)` — returns true if current time is past deadline
- `getSlaStatus(deadline, alertBeforeMinutes)` — returns "ok", "at_risk", or "breached" based on proximity to deadline
- `getSlaProgress(createdAt, deadline)` — returns 0–100 percentage of elapsed time within the SLA window
- Business hours logic: `isWorkingTime()` checks day + hour range, `nextWorkingStart()` advances to next valid working period
- Files changed: `erp/src/lib/sla.ts`
- **Learnings for future iterations:**
  - BusinessHours interface is also defined in `configuracoes/sla/actions.ts` — the lib version is standalone (no server imports) for use in both client and server code
  - SLA progress is clamped to 0–100 using Math.min/Math.max for safe UI rendering
  - Business hours calculation iterates day-by-day through the working window — works for multi-day SLAs
---

## 2026-03-02 - US-068
- Created `erp/src/app/(app)/sac/tickets/dashboard-actions.ts` with `getTicketDashboard(companyId)` server action
- Returns 8 KPI metrics: openCount, inProgressCount, waitingClientCount, resolvedTodayCount, slaBreachedCount, slaAtRiskCount, pendingRefundsCount, avgResponseTimeMinutes
- Returns ticketsByChannel (EMAIL/WHATSAPP/WEB) and ticketsByPriority aggregations
- Uses 11 parallel Prisma queries for performance: 6 count queries, 1 findMany for SLA at-risk, 1 groupBy for priority, 2 raw SQL for channel groups and avg response time
- SLA at-risk calculated by fetching SlaConfig alert thresholds and using getSlaStatus() from sla.ts
- Channel grouping uses raw SQL with LEFT JOIN to channels table, COALESCE null to 'WEB'
- Avg response time uses subquery: MIN(ticket_message.created_at) - ticket.created_at
- Files changed: `erp/src/app/(app)/sac/tickets/dashboard-actions.ts`
- **Learnings for future iterations:**
  - Prisma `groupBy` returns `_count` as a number (not nested) when using `_count: true`
  - Raw SQL with Prisma: use `$queryRaw` with tagged template literals for parameterized queries
  - PostgreSQL `bigint` from COUNT(*) needs `Number()` conversion for JSON serialization
  - For cross-table aggregations (e.g., grouping tickets by channel type), raw SQL is cleaner than multiple Prisma queries
  - SLA at-risk calculation: fetch active non-breached tickets with SLA deadlines, then use `getSlaStatus()` with alert thresholds from SlaConfig
---

## 2026-03-02 - US-069
- Created `ticket-dashboard.tsx` component with 8 KPI cards and 2 horizontal bar charts
- KPI cards: Abertos, Em Andamento, Aguardando Cliente, Resolvidos Hoje, SLA Estourado (red bg), SLA em Risco (yellow bg), Reembolsos Pendentes, Tempo Médio Resposta
- Charts: Tickets por Canal (Email/WhatsApp/Web) and Tickets por Prioridade (Alta/Média/Baixa) using recharts horizontal BarChart
- Integrated into /sac/tickets page above the filters section
- Data loaded via getTicketDashboard server action
- Files changed: `erp/src/app/(app)/sac/tickets/ticket-dashboard.tsx` (new), `erp/src/app/(app)/sac/tickets/page.tsx` (import + render)
- **Learnings for future iterations:**
  - recharts is already installed and used in the main dashboard — reuse BarChart, ResponsiveContainer, etc.
  - For horizontal bar charts, use `layout="vertical"` on BarChart with `type="category"` on YAxis
  - Use `Cell` component from recharts to apply different colors per bar in a single Bar series
  - KPI cards follow pattern: Card > CardHeader (title + icon row) > CardContent (big value)
---

## 2026-03-02 - US-070
- Replaced filter panel with 4 tabs: Todos, SLA Crítico (badge count), Reembolsos (badge count), Meus Tickets
- Added text search across client name and ticket subject
- Added 3 new table columns: Canal (icon), SLA (colored badge + time left), Tags (badges)
- Updated `listTickets` server action with `tab` parameter for tab-based filtering and `search` for text search across client+subject
- Created `getTicketTabCounts` server action for tab badge counts (SLA critical, refunds)
- Expanded `TicketRow` type with `channelType`, `slaStatus`, `slaTimeLeft`, `tags`
- SLA status calculated from `slaResolution`/`slaFirstReply` deadlines with time-left formatting (e.g. "2h30m", "-0h15m")
- Tab filters: sla_critical uses slaBreached OR deadline within 30min, refunds uses `refunds: { some: {} }`, my_tickets uses session userId
- Files changed: `erp/src/app/(app)/sac/tickets/actions.ts`, `erp/src/app/(app)/sac/tickets/page.tsx`
- **Learnings for future iterations:**
  - Shadcn Tabs component uses `Tabs > TabsList > TabsTrigger` from `@/components/ui/tabs`
  - Badge component from `@/components/ui/badge` supports variant prop (default, secondary, destructive, outline)
  - `requireCompanyAccess` already returns the session with userId — no need to import `requireSession` separately
  - Prisma `where.OR` combined with search needs careful handling: use `where.AND = [{ OR: tabFilter }, searchFilter]` when both exist
  - For tab badge counts in Prisma, `refunds: { some: {} }` checks for existence of related refunds
  - SLA time-left formatting: negative values prefixed with `-` for breached tickets
---

## 2026-03-02 - US-071
- Replaced old chat-style message thread with new unified Timeline component in ticket detail page
- Created `ticket-timeline.tsx` with Tabs (Todos default, Email/WhatsApp disabled for US-072/073)
- Timeline shows 4 event types: messages (email/WhatsApp/web), internal notes (yellow bg + lock icon), refund events (coin icon), status changes (gear icon)
- Each event displays: sender name, contact name+role, date/time, channel badge, origin label (via ERP/WhatsApp Web/Email Externo)
- Attachments shown inline with file name, size, and download link
- Removed old reply form, added: internal note textarea + "Comentar" button, "Anexar ao ticket" button with file upload
- Created `listTimelineEvents` server action combining TicketMessage + Refund + AuditLog (STATUS_CHANGE) sorted chronologically
- Created `createInternalNote` server action (isInternal=true, direction=OUTBOUND, origin=SYSTEM) with attachment support and audit log
- Created `attachFileToTicket` server action for ticket-level file attachments via /api/upload
- Files changed: `erp/src/app/(app)/sac/tickets/actions.ts`, `erp/src/app/(app)/sac/tickets/[id]/page.tsx`, `erp/src/app/(app)/sac/tickets/[id]/ticket-timeline.tsx` (new)
- **Learnings for future iterations:**
  - Timeline events are synthesized from 3 sources: TicketMessage, Refund, and AuditLog — merged and sorted by createdAt
  - Internal notes use TicketMessage with isInternal=true, direction=OUTBOUND, origin=SYSTEM
  - Prisma includes `contact` relation on TicketMessage via named relation "TicketMessageContact" — gives name + role for display
  - File upload flow: POST /api/upload → get storagePath → call attachFileToTicket server action to create Attachment record
  - Email/WhatsApp tab content is placeholder (disabled triggers) — to be implemented in US-072/US-073
  - Existing `listTicketMessages` and `createTicketReply` kept in actions.ts for backward compatibility
---

## 2026-03-02 - US-072
- Enabled Email tab in ticket timeline with thread-style layout
- Created `EmailThreadItem` component showing De, Para, Assunto, data/hora, origem for each email message
- Filters events to `channel === "EMAIL"` and `type === "message"` (no internal notes shown)
- Created `getEmailRecipients(ticketId, companyId)` server action — returns client email + additional contacts with emails
- Created `sendEmailReply(ticketId, companyId, to, subject, content, attachmentIds?)` server action — creates TicketMessage with channel=EMAIL, direction=OUTBOUND, origin=SYSTEM
- Reply form: To dropdown (client + contacts), Subject (Re: ticket subject), textarea, attach button, send button
- Attachment support: files uploaded via /api/upload, then linked to message
- Updated TicketTimelineProps to accept `ticketSubject` prop
- Files changed: `erp/src/app/(app)/sac/tickets/actions.ts`, `erp/src/app/(app)/sac/tickets/[id]/ticket-timeline.tsx`, `erp/src/app/(app)/sac/tickets/[id]/page.tsx`
- **Learnings for future iterations:**
  - Email recipients derived from client.email + client.additionalContacts (filtered to those with email)
  - sendEmailReply creates the TicketMessage record immediately; actual SMTP sending will be handled by US-077 worker
  - Email thread items show structured header (De/Para/Assunto/Data) unlike the Todos tab's inline header
  - Attachment state in email reply form is separate from ticket-level attachments — uses local state array
---

## 2026-03-02 - US-073
- Enabled WhatsApp tab in ticket timeline with chat-style bubble layout
- Created `WhatsAppBubble` component: INBOUND messages as left-aligned white bubbles, OUTBOUND as right-aligned green bubbles
- Each bubble shows: sender name+role, content, attachments, timestamp, origin label
- Chat area uses gray background (bg-gray-50) for visual distinction from other tabs
- Created `getWhatsAppRecipients(ticketId, companyId)` server action — returns client phone + additional contacts with WhatsApp numbers
- Created `sendWhatsAppMessage(ticketId, companyId, to, content, attachmentIds?)` server action — creates TicketMessage with channel=WHATSAPP, direction=OUTBOUND, origin=SYSTEM
- Reply form: destination number selector dropdown, textarea, attach button, green-themed send button
- Attachment support: files uploaded via /api/upload, then linked to message via local state array
- Filters events to `channel === "WHATSAPP"` and `type === "message"` (no internal notes shown)
- Files changed: `erp/src/app/(app)/sac/tickets/actions.ts`, `erp/src/app/(app)/sac/tickets/[id]/ticket-timeline.tsx`
- **Learnings for future iterations:**
  - WhatsApp recipients derived from client.telefone + client.additionalContacts (filtered to those with whatsapp)
  - sendWhatsAppMessage creates the TicketMessage record immediately; actual Evolution API sending will be handled by US-078 worker
  - Chat bubble layout uses `justify-start`/`justify-end` with `max-w-[75%]` for natural chat appearance
  - Green send button uses `className="bg-green-600 hover:bg-green-700"` to match WhatsApp branding
---

## 2026-03-02 - US-074
- createInternalNote server action already fully implemented in US-071 (ticket-timeline work)
- Verified: creates TicketMessage with isInternal=true, direction=OUTBOUND, origin=SYSTEM
- Verified: requireCompanyAccess, attachment support via updateMany, audit log
- Typecheck passes — no changes needed
- Files changed: none (already complete)
- **Learnings for future iterations:**
  - US-071 timeline implementation preemptively built the createInternalNote action — check existing code before implementing
  - Always verify acceptance criteria against existing codebase before writing new code
---
